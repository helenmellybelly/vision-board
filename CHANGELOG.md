# Changelog

## 현재 상태
<!-- /wrap이 매 세션 이 섹션을 업데이트합니다 -->
- **상태:** v11 콜라주 타이틀 커스터마이즈 — **로컬 완료·배포 대기** (2026-08-13). 오너 지적 2건: ① "'비전보드 2029'가 다 고정이라 이미지를 가려버린다 — 크기·위치를 조정하고 뒤 배경을 투명하게, '비전보드' 옆에 바로 '2029'가 오게" ② "모바일에서 스튜디오 연도가 너무 작다". **v10의 타이틀은 9점 앵커 × 3스타일 프리셋 덩어리**였고 크기·투명도·표시요소·가로세로가 전부 코드 상수라 손댈 여지가 없었다. v11이 다섯 축을 연다: **크기(70~180% 슬라이더 + 카드 안 ⤡ 핸들) · 배경(불투명/반투명/투명) · 표시 요소(전체/문구만/연도만/숨기기) · 세로↔가로 · 자유 좌표 드래그**. 컨트롤은 **바텀시트**(`TitleSheet`, `StickerSheet`에서 `BottomSheet` 껍데기 추출) — 보드 안 9점 패널은 모바일 보드 폭(390px 화면에서 약 294px)에 6종 컨트롤이 안 들어가고, 페이지 크롬에 붙이면 `--board-reserve` 예산이 늘어 PC 보드 폭 계약(V87-4e)을 건드린다. 시트는 `fixed` 오버레이라 페이지 높이를 0 먹는다. **① 렌더러는 표시 리스트만 그린다** — `titleLayoutFor(cfg, aspect, bgHex)`가 박스·색·줄별 `{x, cy, size, tracking, align, color}`를 내려주고 DOM·canvas는 그리기만 한다(v10은 세로정렬 flex vs `h*0.32/0.68`·연도 자간 0.1em vs 0·`padY` 미사용 **세 곳이 이미 갈라져 있었다** — 이번에 정본 해소). canvas의 "글자 사이 공백 끼우기" 폴백은 `drawTracked()`로 대체(공백 폭이 폰트마다 달라 폴백 환경에서만 카드 밖으로 삐져나갔다). **② 카드 크기는 스타일별 w/h 테이블이 아니라 글자 내용에서 계산** — 그래서 '연도만'이면 카드가 저절로 낮아지고 '가로'면 넓은 스트립이 된다(24칸 테이블을 눈대중으로 채우지 않는다). advance·ink 높이는 `scripts/tune-title.mjs --fonts`가 실제 웹폰트에서 `measureText`로 잰 값(⚠️ 자간은 CSS·canvas 모두 **마지막 글자 뒤에도** 붙는다 = n배, 실측 확인). **③ 모양은 전역, 위치만 기기별** — `BoardData.collageTitle`(스타일·방향·표시·배경·글자색·배율)은 `collageBgColor`와 같은 축이라 템플릿을 바꿔도 따라오고, 위치만 `CollageLayout.title`(`anchor` 또는 자유 좌표 `pos`)에 뷰·템플릿별로 산다. 미설정 필드는 템플릿 기본값으로 접혀 세 템플릿이 계속 서로 달라 보인다(V10-3a 근거). **스키마 v5 불변** — 전 필드 옵셔널 + 멱등 가드 승격이라 기존 데이터 무손실. **④ 경계 클램프는 읽기 시점 한 곳**(`titleLayoutFor` 내부)이라 기기 프리셋 변경·스타일 변경으로 카드가 커져도 보드 밖으로 못 나간다. 크기 상한도 박스를 자르는 게 아니라 **배율을 낮춰** 지킨다(자르면 글자만 커져 넘친다). **⑤ 모바일 크기 상수는 스윕 역산** — `TITLE_UNIT_BETA 0.95`/`FMAX 1.5`/`BASE_SCALE 1.25`는 `scripts/tune-title.mjs`가 2,772조합을 쓸어 고른 값(목적함수: "연도 ink 높이 / 보드 대각선"의 **템플릿별 변동계수 평균** 최소화, 하드 제약: 화면 라벨 ≥11px·화면 연도 ≥20px·내보내기 연도 ≥28px·카드 면적 ≤보드 14%). 짧은 변 순수 비례가 존재감을 보존 못 해 폰 0.022 ↔ Z플립커버 0.0365로 **1.66배** 벌어져 있던 게 "모바일에서만 작다"의 실체 — 채택안에서 **1.24배**. 폰 미리보기 라벨 9px → 16.5px. ⚠️ `titleUnit` 하한을 1로 클램프하지 않으면 정사각에 가까운 보드가 −24%로 오히려 작아진다. ⚠️ **기본 배경을 `soft`(반투명 55%)로 변경** — 기존 보드 생김새가 한 번 바뀐다(오너 승인). 최악 사진 양쪽(검정/흰색)에서 대비 ≥3.0을 계약으로 고정하고, ⚠️ **알파 합성은 sRGB 감마 공간에서** 계산한다(선형광으로 재면 다크 반투명이 2.09로 나와 알파를 0.75까지 올리게 된다). ⚠️ 반투명·투명 카드에서는 v10의 라벨 회색(`#6E6962`)을 쓸 수 없다 — 검은 사진이 비치면 대비 1.62까지 떨어진다(verify-title ⑩이 실제로 잡아냄). ⚠️ **편집 모드에서 잡히는 건 카드가 아니라 글자다** — 카드 전체를 잡게 두면 카드가 덮은 사진을 아예 탭할 수 없다(V10-7a·v81r2가 실제로 그렇게 깨졌다). ⤡ 핸들도 카드 **안쪽**에 두고 표식은 `data-title-resize`(≠`data-resize-for`) — 그 속성은 items 항목 핸들이라는 뜻이라 끼면 기존 스위트의 `.last()`가 타이틀을 집는다(V85-8d 실측). ⚠️ **`scripts/tune-title.mjs`는 `effScale`로 정규화해야 재실행 가능** — 안 그러면 이미 박은 상수가 기준선에 섞여 이중 적용된다. 검증: `scripts/verify-title.js` 신설 **26**(3.8만 조합 스윕 — 클램프·오버플로·대비·타이포 스냅샷), `.claude/verify-v11r1.mjs` 신설 **41**, `run-all-v86.mjs`에 verify-title 등록. **로컬 32/32 스위트 PASS**, tsc 클린, 신규 lint 0. 계약 반전 1건(V10-4a 패널→시트, 저장 경로 불변). ⚠️ **부수 발견: V10-7a는 v11 이전부터 깨져 있었다**(HEAD에서 동일 재현) — 핸드오프의 "로컬 30/30"은 이 환경에서 성립하지 않았다
- **직전(v10):** v10 콜라주 방향 인식 재설계 — **프로덕션 라이브** (커밋 6230a5e, 2026-08-12). 오너 지적 3건 해결: ① 모자이크·미니멀이 거의 같다 ② 매트 갤러리를 없애야 한다 ③ 세로 사진이 가로 자리에 들어가 안 보인다. **②와 ③은 같은 문제의 앞뒤였다** — 배치 엔진에 사진 치수가 전혀 흐르지 않아(스키마에 width/height 필드 자체가 없었다) 모든 셀을 픽셀 정사각으로 가정하고 `object-cover`로 잘랐고(세로 사진 40~55% 손실), 매트 갤러리(흰 액자+`object-contain`)는 그 **유일한 회피책**이었다. 대가가 "액자 안에서 작아 보임". 치수를 알게 되자 액자의 존재 이유가 사라졌다. **①의 재발 방지: 템플릿을 알고리즘이 아니라 완성된 구성안으로 재정의** — 배치 구조 + 타이틀 카드 위치·스타일 + 기본 스티커 킷 + 갭이 함께 갈린다(여백 수치로만 구분하면 사진이 작아지거나 차이를 못 느낀다. v9 모자이크↔미니멀이 갭 0.025 vs 0.03 차이뿐이었던 게 그 증거). **에디토리얼**(순서대로 조밀·풀블리드·정중앙 밴드 타이틀) / **매거진**(한 장이 주인공·무크롭 히어로·좌상단 볼드) / **스튜디오**(방향별 밴드 정렬·상단 가운데 라인). 타이틀 앵커 9점 × 스타일 3종을 사용자가 고르고, **상단 예약 밴드 폐지로 사진 면적 15~22% 증가**. ⚠️ **피할 수 없는 사실**: 고정 캔버스에서 "원본 비율 100% + 채움률 100%"는 동시에 성립하지 않는다(행이 폭을 채우는 순간 높이가 결정돼 자유도가 사라진다) — 잔차는 여백·균일 스케일(=대칭 중앙 크롭)·블러 배경 중 하나로만 흡수된다. 실사용 30셀(폰·PC × 3템플릿 × 6~18장) 실측 **채움률 0.93~0.98 · 크롭 ≤6%**(v9는 40~55%). 1~3장처럼 수학적으로 불가능한 구간은 앰비언트 블러. 스키마 v4→v5(구 배치 폐기·사진/색/기기 사이즈 보존·스티커 문구 salvage·1회 안내). ⚠️ **로그인 사용자는 v10 첫 진입 때 병합 시트를 한 번 볼 수 있다**(로컬은 마이그레이션됐는데 서버 보드는 v4 모양 — 병합 로직 자체는 무손상). 로컬 30/30 스위트 PASS(기하 283 + 브라우저 29종), prod v10r1 30/30+스모크 20/20+v87r1 41/41+v86r1 16/16+v84r1 28/28. 보안 점검 confidence ≥8 findings 0
- **직전(v9.0):** v9.0 콜라주 그리드 재설계·매트 갤러리·배경색 팔레트·온보딩 가치 전달 — **프로덕션 라이브** (커밋 6d344b1, 2026-08-11). 오너 피드백 6건 해결: ① 크기 조절이 수동이라 힘들다 — **배치의 진실 원천을 좌표 역산 → 명시적 `GridSpec`으로 교체**(`lib/collageGrid.ts` 신설). v8.2~v8.7은 `inferGridSpans`가 좌표에서 그리드를 역산했고 실패하면 리플로우가 통째로 죽어 "한 번 자유롭게 옮기면 그 뒤로 자동 정렬이 영영 안 됨"이 됐다. 이제 빈틈 0이 측정이 아니라 **정리(theorem)**: row 밴드는 `sum(spans)===cols`, hero 밴드는 `1+2×(cols−2)`가 정확히 cols 점유. `items`를 항상 `grid`와 일관되게 기록하므로 구 코드가 읽어도 정상 렌더(**스키마 v4 불변**) ② PC가 "너무 왁! 큰 느낌" — **버그가 아니라 사양이었다**: cols 탐색 목적함수가 `argmax cellW`(셀 폭 최대화)라 알고리즘이 '크게 채우기'를 최적화했고 외곽 여백 38px이 갭 29px과 동급(1.3:1)이라 사진이 화면 폭의 96%를 먹었다. **채움률 목적함수 + `photoBounds` 제약으로 분리**(반대 방향인 두 요구는 이렇게만 동시 성립), 여백 81px(3:1)·가로형 연도 76→56px, 여백을 minDim 비례로 통일해 축 비대칭 해소 ③ 숲 삭제 → **매트 갤러리**(넓은 매트·균일 1×1·**무크롭 `object-contain`** — 세로 스크린샷이 정사각 히어로에서 잘리던 문제의 완전 해소) ④ **배경색 팔레트 8색을 세 템플릿 공통으로**(`collageBgColor` 전역 1개, `COLLAGE_THEMES` 상수 → `themeFor(template,bgColor)` 함수 승격 — dark가 배경색 파생값이 되어 상수로 표현 불가) ⑤ 가이드 문구가 안 보임 → 보드 **위** 알약으로 승격 ⑥ /collage의 '미래 일기 읽기' 삭제(대시보드는 유지). 온보딩 Step3에 과정 3컷+차별점(스텝 수 불변). ⚠️ **계약 변경: 그리드 모드에서 사진 추가 시 기존 좌표가 움직인다**(v8.x "기존 rect 불변"과 충돌 — 보존되는 건 읽기 순서). 조판 수치는 `lib/collageTokens.ts` 단일 소스(구 titleBottom 5중 중복 제거). 기하 계약 재작성 **1709/1709**(구 453), verify-v90r1 신설 42/42, 기존 28종 전부 PASS, prod v90r1 42/42+v87r1 41/41+v85r1 32/32+v84r1 28/28
- **v8.7:** 이미지 로드 근본 수정·붙여넣기 URL 정규화·콜라주 컨트롤 재배치 (커밋 acf45c6, 2026-08-11). 오너 피드백 2건 해결: ① 배경화면 저장 시 "사진 8장은 못 불러와서 빠졌어"(PC·모바일 공통) — 원인은 UI가 아니라 파이프라인 구조로, 표시(DOM `<img>`, crossOrigin 없음)와 내보내기(canvas, `crossOrigin='anonymous'`)가 **같은 사진을 다른 URL·다른 CORS 모드**로 요청해 브라우저 HTTP 캐시 키가 갈라짐 → 저장 때 전량 재요청 → 프록시 동시 폭주로 일부 타임아웃. `lib/imageSrc.ts displaySrc()`를 표시 4곳+캔버스 로더의 단일 관문으로 세워 동일 출처 프록시 URL로 통일(캐시 엔트리 1개·taint 0·내보내기 네트워크 ≈0), loadOne 재작성+dedupe+동시성 6+데드라인 20s+skippedKeys, 프록시 immutable CDN 캐시·리다이렉트 3홉 재검증·스트리밍 실바이트 상한·실패 no-store, `rateLimited(req,max,bucket)` 버킷 분리. **부수 효과: v8.1의 깨진 사진 경고 3중 장치가 처음으로 실제 발화**(그전엔 DOM만 성공해 구조적으로 불발) ② 기기 사이즈 칩이 화면 밖으로 잘림 — 템플릿 탭 바로 아래 전폭으로 이동+lg wrap, 폰 뷰 lg 레일 폐지, lg 헤더 1행 통합(≈52px 회수), PC 뷰 무스크롤(1280×900)+보드 1088px+18장 최소 셀 101px 동시 성립. 붙여넣기 URL은 수입 즉시 내 저장소로(`/api/image/fetch` — 동일 출처 게이트·DNS 사설IP 차단·SVG 금지·8MB, `importRemoteImage` 단일 관문, `lib/imageNormalize` 온디맨드 복구). 보드 스키마 v4 불변. verify-v87r1 신설 41, 전 스위트 29/29(기하 453), prod v87r1 41/41+스모크 20/20+v84r1 28/28+v86r1 16/16+PUT 401. ⚠️ 기존 붙여넣기 URL 사진은 "내 보드로 가져오기" 1회 필요
- **v8.6:** 동기화 스탬프 병합 판정·로그아웃 완료 화면 (커밋 3885a28, 2026-08-01). 오너 피드백 2건 해결: ① MergeSheet 오탐 — `decideMerge` v2(내용 동등(stable stringify+휘발 필드 제외 — jsonb 키 순서 무관)→useLocal, 서버==마지막 푸시 스탬프→useLocal, 로컬 무변경+서버 앞섬→useServer, 양쪽 독립 변경만 ask), `lib/syncStamp.ts` 신설(vb-board-rev·vb-board-modified-at·vb-synced-local-rev/server-at — 보드 스키마 v4 불변), PUT /api/board가 updated_at(epoch ms) 반환, saveLastVisit는 skipRevBump(마운트 휘발 저장의 rev 오염 방지), "나중에 정할게" 보류 지속 vb-merge-deferred(리로드 조용 덮어쓰기 버그 수정), 동기화 강건성(runSync 재큐잉·5s 1회 재시도·pagehide/visibilitychange 플러시 keepalive) ② 로그아웃 체감 무변화 — `/logged-out` 완료 화면 신설(보드 유지 안내+재로그인)+로그아웃 시 세션 스탬프 정리·signOut 실패에도 이동. verify-v86r1 신설 16(로컬+prod 각 16/16), 전 스위트 28/28(기하 453), prod 스모크 20/20, 비로그인 PUT 401 불변. ⚠️ 스탬프 기록 이전의 기존 서버 보드는 최초 1회 ask가 의도
- **v8.5:** 완주 피드백 리플래닝 (커밋 87efbdb, 2026-07-31). 오너 피드백 10건 해결: ① 미래 일기 일본어 혼입("유기のか") — `lib/honorific hasForeignScript`(가나·한자·키릴, 라틴 제외)+공용 `lib/storyGate`(재생성 후 재검증·잔존 시 기계 제거, LLM 최대 2회)+프롬프트 락스텝 줄+STORY_PROMPT_VERSION 3+/api/summarize·image/describe도 게이트, ② 다듬기 인라인화 — /diary 안에서 "AI로 다시 쓰기"(확인 배너·무제한)+"직접 수정하기", /finish는 최초 생성 전용, ③ 섹션 일기도 탭에서 AI 수정(2회 캡 공유)+직접 수정, ④ /diary 헤더 oneSentence 승격+날짜 탭 변경(보드 날짜 1개), ⑤ 탭바 flex-wrap(슬라이드 제거)+전체 탭 6편 부록 삭제+탭 전환 스크롤 리셋, ⑥ 대시보드 카피 교체("비전보드는 계속 가꿔 나갈 수 있어")+완주 walkCaption 삭제+북극성 한 문장·D-day 줄(→/diary), ⑦ 프로필 아이콘 도토리 모티프, ⑧ /collage 하단 이야기 블록 삭제→일기 링크 1줄, ⑨ MergeSheet 설명력(왜 떴는지·일기/사진 요약·시각 fmt), ⑩ 미니멀 혼합 그리드(밴드 타일링 — 크고 작은 혼합, 스팬≤4 락스텝)+모자이크 반자동 리사이즈(비정사각 스냅·드래그 중 고스트 프리뷰·자유 배치 안내). verify-v85r1 신설 32 + 갱신 6스위트(v75r1·v78r1·v79·v81r1·v83r1·v84r1) + verify-collage-layout 453(§8/§9 신 계약) + 로컬 전 스위트 PASS, prod 스모크 20/20+v83r1 33/33+v84r1 28/28+v85r1 32/32+스토리 실호출 9/9(S9 외국 문자 포함)
- **기준 플랜:** `~/.claude/plans/editorial-magazine-studio-virtual-nova.md`(v11 전 단계 완료·**배포 대기**). 다음: ① `npx vercel --prod` 배포 여부 오너 결정(GitHub 자동배포 없음) ② v11 오너 육안 — `verify-shots/title-v11-*.png` 11장은 **단색 플레이스홀더**라 구조만 보인다. 실사진으로 반투명 카드가 사진을 충분히 살리는지 / 기본 1.25배가 과한지(과하면 `collageTokens TITLE_BASE_SCALE`) / 가로 배치·투명 배경이 쓸 만한지. 이전 플랜: `~/.claude/plans/breezy-juggling-key.md`(v10). 이전 잔여: v10 육안 — `verify-shots/collage-v10-*.png` 10장은 **단색 플레이스홀더**라 구조만 보인다. 실사진으로 ① 세 템플릿이 충분히 다르게 느껴지는지 ② **매거진 18장에서 히어로가 보드 절반을 먹는 게 "주인공"으로 읽히는지 과한지**(과하면 `collageTokens heroMaxWFor`의 매거진 0.6을 낮춘다) ③ 기본 스티커 킷이 도움인지 거슬림인지 ④ 모바일 드래그 스왑·크게/작게 조작감. 이전 잔여(v8.6 MergeSheet·/logged-out 육안, v8.5 육안 5건, 실계정 복원 확인, Vercel Web Analytics)
- **주요 기능:**
  - 🐿️ 토리 캐릭터 (꿈의 정원사) — Acts 0-5 구조, 온보딩 전체 뷰포트 고정, 3뷰포트(375×667/390×844/1280×720) 무스크롤. v7.6: 이름 용도 힌트·"첫 스테이션은 5분" 기대값·도토리 채팅 딤 제거
  - 한국어 조사: `lib/josa.ts` 단일 소스(아/야·이/가·은/는·을/를·으로/로 ㄹ특례·이라는/라는, 비한글 fallback 무받침형) — 온보딩 이름 보간 + 씬 브리지/쿠션 키워드 조사
  - 디자인 시스템 (`docs/design-system.md`): 타입 스케일 7토큰 + UI Pretendard 단일 / 아트 Enjoystories(.font-script, collage 전용) (`scripts/check-typography.js` 기계 검증). **v7.6 숲 서피스 팔레트 `lib/colors.ts FOREST`**(deep #1F2E22→light #2A3D2E, card #33473A) — 산책길·미니보드·콜라주·배경화면 공유 단일 소스
  - 팔레트: 비비드 6색 `lib/colors.ts` 단일 소스 + 색 사용 문법 통일(색은 도트 전담, 텍스트·보더는 뉴트럴)
  - 대시보드 = 산책길 허브 (`components/WalkPathMap.tsx`): 숲 그라디언트 패널 위 도토리 출발→6스테이션(🌰/💬/📷/🌳+완료 칩+🌿 수풀, v7.7 `lib/stationStatus.ts` 단일 소스+title 툴팁+완료 구간 실선)→참나무 언덕. **첫 방문 "여기서 시작 ▾" 바운스 라벨 + 확산 글로우 링**(진행 생기면 라벨 소멸, reduced-motion 대응). aria-label 계약 `${label} — ${STATUS_LABEL}` 유지. pathSheet에 선택지별 기대값 서브라인
  - `/collage` (v10 방향 인식 재설계): 전 뷰포트 [폰|PC] 2탭+보드 전폭+sticky 저장 바, 템플릿 3종(**에디토리얼**=순서대로 조밀·풀블리드·정중앙 밴드 타이틀 / **매거진**=한 장이 주인공·무크롭 히어로·좌상단 볼드 / **스튜디오**=방향별 밴드 정렬·상단 가운데 라인) + **배경색 팔레트 8색 공통** + **타이틀 앵커 9점 × 스타일 3종** + **템플릿별 기본 스티커 킷**(지우면 기억, '기본 배치로'면 복구). 층 구조: `lib/collageJustify.ts`(순수 기하·아무것도 import 안 함) → `lib/collageSolve.ts`(정책) → `lib/collageTemplates.ts`(보드 데이터) → DOM/canvas 2렌더러. 사진 실측 치수는 `lib/imageDims.ts`가 `BoardData.photoDims`에 저장(⚠️ `displaySrc()` 경유·`crossOrigin` 미설정 필수). 편집: 이동=스왑·리사이즈=행 경계 이동·탭 액션 크게/작게(불가능하면 비활성)·자유 배치 토글(여기서만 회전). 편집 모드 출처 섹션 배지+사진 인라인 교체/삭제+깨진 사진 ⚠️(v8.1), 스티커 문구 18종+라인 아이콘 7종(`lib/stickerArt.ts` 단위 path를 DOM `<svg>`·canvas `Path2D`가 공유), 기기 프리셋 11종
  - 질문 플로우 (v7.7): 예시 `examples: string[]` 5세트(결핍·유지·성장·전환기·관계공동체 페르소나, 구체 명사/숫자) + "다른 예시 보기 ↻" 로테이션 + 형식형 플레이스홀더, want 복수 유도(3줄 멀티라인). **씬 브리지**: 방향 키워드 후 섹션 채팅 안에서 "미래의 하루 일기" 기대값 안내 버블→CTA로 /scene 진입, 씬 예시 산문/불릿 2형식
  - 하루 일기 (v7.3): 사용자 장면 중심 프롬프트(450-650자), scene 작성 가이드 카드(3행 — 형식 자유 포함)
  - 분석: @vercel/analytics 페이지뷰 + track 5종(커스텀 이벤트는 Pro 플랜 전용)
  - 이미지 추천: 섹션 채팅 첫 답변 직후 Unsplash 추천 4장 → 탭하면 슬롯 저장 → /collage 즉시 반영
- **알려진 이슈:** v11은 **아직 배포 전**이다(로컬 32/32 PASS, prod는 v10 6230a5e). 배포하면 기존 사용자의 타이틀 카드가 반투명·1.25배로 한 번 바뀐다(의도된 기본값 변경). 편집 모드에서 타이틀 **글자**가 덮은 자리의 사진은 탭이 안 된다 — 타이틀을 옮기면 된다(카드 여백·투명 영역은 통과한다). 로그인 사용자는 v10 첫 진입 때 병합 시트를 **한 번** 볼 수 있다(마이그레이션이 로컬 보드를 바꾸는데 서버 보드는 아직 v4 모양 — "이 기기 것으로 계속"을 고르면 된다. 병합 로직 자체는 무손상, verify-v86r1 16/16). 에디토리얼이 풀블리드라 보드에 '빈 곳'이 없다 — 편집 진입의 결정적 동선은 상시 `✎ 탭해서 편집` 버튼뿐이다. hydration #418은 전 페이지 공통 useState(loadBoard()) 패턴의 기존 이슈(표시는 정상). 폰 뷰를 PC에서 열면 세로 스크롤이 남는다 — 9:19.5 보드를 ≥300px로 유지하면서 1280×900에 넣는 건 기하학적으로 불가(보드만 649px+크롬 383px). PC 뷰는 무스크롤. 로그인 사용자의 원격 사진 표시가 우리 함수/대역폭을 경유한다(immutable CDN 캐시로 URL당 사실상 1회 — 대역폭 모니터 필요). public에 미사용 한글 파일명 에셋 잔존(정리 대상). verify-v614의 팔레트 대비 정적 검사는 비비드 복원으로 FAIL이 정상.

## 세션 로그
<!-- ⚠️ APPEND ONLY — 아래 항목을 절대 삭제/수정하지 마세요. 새 항목은 이 줄 바로 아래에 추가합니다. -->

### 2026-08-13 (v11 — 콜라주 타이틀 커스터마이즈, 로컬 완료·배포 대기)
- 오너 지적 2건(타이틀이 고정이라 이미지를 가린다 / 모바일에서 연도가 너무 작다) → 탐색 2병렬 + 설계 에이전트 + 오너 결정 6건(자유 드래그+9점 프리셋 / 모양은 전역·위치는 기기별 / 바텀시트 / 문구 고정 / 기본 반투명 / 에디토리얼 정중앙 유지)
- **설계 결정 4개**: ① 스키마 v5 유지(전 필드 옵셔널 + 멱등 승격 — 비멱등 연산이 없다) ② 드래그 좌표를 `items['title']`이 아니라 `layout.title.pos`에 — `items` 순회 지점 **7곳**(`photoAtPoint`·`withStickers`·`resolveLayout` freeform·`applySpec`·wallpaper z정렬·`bringToFront`·`isLayoutBroken`)이 타이틀을 사진으로 오인한다. 하나만 놓쳐도 조용한 데이터 손상 ③ 카드 크기를 (style×dir×parts) 24칸 테이블이 아니라 **글자 내용에서 해석적으로** 계산 ④ 렌더러는 **표시 리스트만** 그린다
- 구현: `collageTokens`에 `TitleConfig`/`TitleLayout`/`titleLayoutFor`/`resolveTitleConfig`/`compositeContrast`/`titleUnit` 신설(무의존 순수 계약 유지), 렌더러 2종 전환 + v10의 락스텝 균열 3곳 해소, `BottomSheet` 추출 → `TitleSheet` 신설, 드래그 엔진에 타이틀 분기 1개, `BoardData.collageTitle` + `saveCollageTitle` + `migrateCollage` 멱등 승격
- **가드레일 눈대중 금지 실천**: 폰트 advance는 Playwright `measureText` 실측(자간이 n배임을 확인), 크기 상수는 2,772조합 스윕 역산. 첫 두 스윕은 통과 후보 0건 — 하한을 ink 높이로 잰 것(폰트 크기여야 함)과 상한 클램프를 위반으로 센 것(실제 코드는 배율을 낮춘다)이 원인이었다
- **회귀 3건을 스위트가 잡았다**: (a) 타이틀 ⤡ 핸들이 카드 **밖**에 떠서 사진 탭을 삼킴 → 카드 안으로 (b) 카드 전체가 드래그 대상이라 덮인 사진을 못 누름 → **글자만** 잡게 (c) 핸들 표식이 `data-resize-for`라 기존 스위트의 `.last()`가 타이틀을 집음 → `data-title-resize`로 분리
- **부수 발견 2건**: V10-7a는 v11 이전부터 실패(HEAD 재현 확인 — 액션 칩 토글을 테스트가 스스로 닫고 있었다), v81r2는 앰비언트 배경 img를 사진으로 알고 클릭 중이었다(타이틀이 투명해서 우연히 통과)
- 검증: `scripts/verify-title.js` 26 + `.claude/verify-v11r1.mjs` 41 신설, 로컬 **32/32 스위트 PASS**, tsc 클린, 신규 lint 0. 스크린샷 11장을 Read로 열어 육안 확인(반투명·가로 배치·투명 글자·canvas 락스텝)

### 2026-08-12 (v10 — 콜라주 방향 인식 재설계·템플릿 3종 교체, 프로덕션 배포 완료)
- 오너 지적 3건(모자이크·미니멀이 거의 같다 / 매트 갤러리를 없애야 한다 / 세로 사진이 가로 자리에 들어가 안 보인다) → 탐색 2병렬 + 설계 에이전트. **②와 ③이 같은 문제의 앞뒤임을 코드에서 확인**: 저장 스키마(`lib/types.ts`)에 width/height 필드 자체가 없어 엔진이 모든 셀을 픽셀 정사각으로 가정했고(`unitH = unitW * aspect`), 유일한 예외인 첫 사진 1장의 비율마저 "2×2 히어로를 쓸까" 판정에만 쓰였다. 매트 갤러리는 그 회피책이었을 뿐이다
- 착수 전 전제 3건을 명시하고 승인받았다. 특히 **"원본 비율 100% + 채움률 100%는 고정 캔버스에서 동시 성립 불가"** — 행이 폭을 정확히 채우는 순간 높이가 결정돼 자유도가 사라진다. 잔차는 여백·균일 스케일(대칭 중앙 크롭)·블러 배경 중 하나로만 흡수된다. 균일 스케일이 최적인 근거: `Σh_i = T` 제약에서 `max|log(h_i/자연)|`을 최소화하는 해가 `h_i = s·자연`
- 구현 A(치수 파이프라인, `lib/imageDims.ts`): 배치 엔진의 **선행 작업**이다 — ratio가 전부 1이면 솔버는 v9 정사각 격자와 수학적으로 같은 답만 낸다. 업로드·AI생성·붙여넣기·수입 시점 저장(`compressImage`가 이미 img를 로드 중이라 추가 비용 0) + `/collage` 마운트 백필. ⚠️ `savePhotoDims`는 `skipRevBump`(v8.6 계약), `merge.ts VOLATILE_BOARD_KEYS`에 `photoDims` 추가
- 구현 B(저스티파이드 엔진, `lib/collageJustify.ts`+`collageSolve.ts`): 같은 행은 높이를 공유하고 폭은 원본 비율에 비례 → 세로는 좁고 길게, 가로는 넓고 낮게. `JustifiedSpec{order, hero?, rows[]}`이 v9 `GridSpec` 대체 — **블록·밴드 구조가 없다**: 스튜디오의 방향별 밴드는 자료구조가 아니라 order를 방향별로 정렬한 **결과**다(세로만 모인 행은 Σr이 작아 저절로 높아진다). 덕분에 왜곡 스케일 s가 보드 전역에서 하나 = 크롭이 전 사진 균일. 편집 연산은 **order를 건드리지 않는다** — 행은 순서열의 절단점일 뿐이라 "이 행의 마지막을 다음 행으로"가 `rows[r]−1, rows[r+1]+1`이다
- 가드레일 역산(`scripts/tune-justify.mjs`, 7비율×3템플릿×5믹스×18장=1890건): `minPhotoW` 0.085는 16:9에서 7~8장 행을 전멸시켜 n=18 채움률을 **0.64**로 떨어뜨렸고(→0.05), `maxRowH` 0.62(v9 `photoBounds` 유산)는 세로 보드에서 후보를 전멸시켜 앰비언트를 63% 발동시켰다(→0.85). 결과: 실사용 30셀 채움률 0.89~0.98·크롭 ≤0.06
- **검증이 전부 초록인데도 스크린샷을 열어보고서야 결함 8건을 찾았다.** 가장 심각한 건 매거진에 히어로가 **아예 안 생긴 것** — `heroSplit`이 `ratio < 1`로 세로 분기를 걸어 정확히 1.0(정사각·치수 미측정 기본값)이 두 분기 사이로 빠졌고, 결과적으로 매거진이 에디토리얼과 같은 그림을 그렸다(①의 재발). 고치자 스튜디오까지 히어로를 골라 매거진과 비슷해졌고, 그걸 고치자 히어로가 보드의 53%를 먹어 나머지 17장이 0.6%까지 잘아졌다(→ `heroMaxWFor` 템플릿별 분리, 2.7% 회복). 그 외 라인 아이콘이 `cqi` 단위 오해로 50px 선이 되어 검은 덩어리, 스티커 하단 잘림, **포인터 캡처를 핸들에 걸어 보드 밖으로 나가면 드래그가 끊김**(풀블리드라 실사용에서 자주 발생 → 캡처를 보드로), 리사이즈 의도를 클램프된 결과 폭으로 재던 것(→ 원시 드래그 거리), '크게' 무반응(→ 불가능하면 칩 비활성)
- PC 세로 예산 재역산: 타이틀 밴드 예약이 사라져 보드가 커진 만큼 세로가 모자라졌다. 예산을 키우면 폭이 1000 밑으로 떨어지므로 **먼저 크롬을 깎고**(헤더·CTA·저장 바·패널 패딩 lg 축소, 가이드 알약을 보드 안 플로팅으로) 남은 7px만 예산으로 흡수(20→20.5rem). 900px 뷰포트에서 `scrollH` 900 / 보드 1017px로 V87-4d·4e 동시 성립
- 검증: `scripts/verify-justify.js` 283 신설(구 `verify-collage-layout.js` 1709 대체·삭제), `.claude/verify-v10r1.mjs` 30 신설, `verify-v90r1`은 `.claude/archive/`로 이관. 기존 스위트 9종 갱신(v7r5 R5-8은 "기존 배치 무손실"→"배치 폐기·사진/색/사이즈 보존"으로 계약 반전, v82r1 V82-4는 "최소 폭 100px"→"최소 면적 2.5%", V82-8a는 "배경 탭 편집 진입"→"사진 탭 확대"로 반전). 로컬 30/30, prod v10r1 30/30+스모크 20/20+v87r1 41/41+v86r1 16/16+v84r1 28/28. 보안 점검 confidence ≥8 findings 0

### 2026-08-11 (v9.0 — 콜라주 그리드 재설계·매트 갤러리·배경색 팔레트·온보딩 가치 전달, 프로덕션 배포 완료)
- 오너 피드백 6건(크기 조절이 수동이라 힘듦 / PC가 "왁!" 크고 세련되지 않음·세로 사진 잘림 / 숲 삭제하고 배경색 바꿀 수 있는 새 템플릿 / 온보딩에 차별점·과정·결과 / 콜라주의 미래 일기 링크 불필요 / 가이드 문구가 안 보임) → 탐색 3병렬+설계 에이전트. **"왁!"은 버그가 아니라 사양이었다**: cols 탐색 목적함수가 `argmax cellW`(셀 폭 최대화)라 알고리즘이 '크게 채우기'를 최적화 중이었고, 외곽 여백 38px이 갭 29px과 동급(1.3:1)이라 사진이 화면 폭의 96%를 먹었으며, n=6에서 2×2 히어로가 화면 높이의 67%(720px)를 차지했다. 오너 확정 4건(매트 갤러리·항상 꽉 채우기·3단계 안에 녹이기·큐레이션 팔레트) + 추가 요청(배경색을 모자이크·미니멀에도)
- 설계 핵심 **목적함수와 제약의 분리**: "빈틈 없이 꽉"(채움률)과 "왁! 하지 않게"(`photoBounds`)는 반대 방향이라 하나는 목적함수, 하나는 탈락 조건이어야 둘 다 성립한다. ⚠️ 가드레일 상수는 눈대중 금지 — 솔버를 n=1..18 × 3비율로 돌려 역산했고(초안이 제시한 landscape maxH 0.36은 자기 계산과 이미 모순), `verify-collage-layout.js`가 회귀를 고정한다
- 구현 A(그리드 대수, `lib/collageGrid.ts` 신설): 배치의 진실 원천을 좌표 역산 → 명시적 `GridSpec`. row 밴드 `sum(spans)===cols` + hero 밴드 `1+2×(cols−2)`가 정확히 cols 점유 ⇒ **빈틈 0이 측정이 아니라 정리**. `items`를 항상 grid와 일관되게 기록해 구 코드 하위호환(스키마 v4 불변). 편집 연산은 "국소 편집 + 하류 재구성"(`resizeInGrid`·`swapInGrid`·`insertKeys`/`removeKeys`) — 구현 중 실제 버그 2건 발견: 축출 인덱스가 어긋나 같은 키를 두 번 밀어내던 것, 스팬 캡 초과 시 편집이 null로 죽던 것(후자는 v8.x 실패 모드의 재현 — `allowCenter`로 실패 대신 여백)
- 구현 B(조판 토큰, `lib/collageTokens.ts` 신설): titleBottom 공식만 5곳에 중복돼 있던 것을 단일 소스로. 여백을 **minDim 비례**로 통일(구 폭 정규화는 16:9에서 좌우 38/상하 32px 비대칭), FHD 여백 38→81px(1.3:1→3:1), 가로형 연도 76→56px(밴드 내 아래 여유 11→43px). 실렌더 확인 중 `splitCols`가 항상 앞쪽에 큰 값을 줘 넓은 사진이 첫 열에 세로로 줄지어 "줄무늬"로 보이는 것을 발견 → 행마다 스팬 배열 회전(합·캡 불변이라 타일링 정리 유지)
- 구현 C(매트 갤러리·배경색): 숲(polaroid) 삭제 → `matte`(넓은 매트 여백·균일 1×1·**무크롭 object-contain** — 세로 스크린샷 잘림의 완전 해소이자 이 템플릿의 컨셉). `migrateCollage`가 polaroid→matte 이관+구 배치 폐기(회전·지터 좌표라 재해석 불가, 사진은 무손실)+1회 안내 배너. **배경색을 세 템플릿 공통 축으로**: `collageBgColor` 전역 1개(템플릿을 바꿔도 따라와야 "내 보드 색"이 성립), `COLLAGE_THEMES` 상수 → `themeFor(template,bgColor)` 함수 승격(dark가 배경색 파생값이라 상수로 표현 불가), 다크 배경에서 글자색 자동 반전+사진 그림자→밝은 링(그림자는 다크에서 안 보인다)
- 구현 D(인터랙션·그 외): 이동=스왑(스팬은 사진이 아니라 '자리'에 귀속)·리사이즈=스팬 등급 1..4 스냅·탭 액션 크게/작게(모바일 주 동선)·자유 배치 토글이 명시적 탈출구. 회전은 자유 배치·스티커에만(회전 bbox가 이웃 셀을 침범해 "빈틈 0"과 논리적 모순) → `freeformHint` 토스트는 도달 불가 경로가 되어 삭제. 가이드 문구를 보드 아래 회색 잔글씨 → 보드 **위** 알약으로 승격, /collage 일기 링크 삭제, 온보딩 Step3에 과정 3컷(답하면→담으면→저장하면)+차별점 한 줄
- 검증·배포: `verify-collage-layout.js` 전면 재작성 **1709/1709**(구 453) — 신규 블록: 타일링 정리·가드레일 회귀·여백:갭≥2·**자동정렬 불사**(v8.x null 사망 지점의 회귀 테스트)·스왑·매트 균일·배경색 대비 24조합. `verify-v90r1` 신설 42/42, `check-wallpaper-v76` 재작성(숲 캔버스 → 배경색 락스텝 픽셀 샘플링) 5/5, 기존 스위트 28종 전부 PASS → master 푸시+`npx vercel --prod`+prod v90r1 42/42·v87r1 41/41·v85r1 32/32·v84r1 28/28. ⚠️ **계약 변경 2건**: 그리드 모드에서 사진 추가 시 기존 좌표가 움직인다(보존되는 건 읽기 순서), `--board-reserve` PC 17→20rem(팔레트 행·힌트 알약 추가분 — 무스크롤과 보드 폭 ≥1000px은 반대 방향이라 테스트로 역산). ⚠️ 온보딩 카피 "막연한 바람이"가 슬라이더 라벨 "막연한 바람"과 겹쳐 strict 매칭을 깬 것을 기존 스위트가 잡아냄

### 2026-08-11 (v8.7 — 이미지 로드 근본 수정·붙여넣기 URL 정규화·콜라주 컨트롤 재배치, 프로덕션 배포 완료)
- 오너 피드백 2건(PC·모바일 모두 배경화면 저장 시 "사진 8장은 못 불러와서 빠졌어" / 기기 사이즈 칩이 화면 밖으로 잘림) → 탐색 3병렬 + 설계 에이전트로 원인 확정. **진짜 원인은 UI가 아니라 이미지 소스 파이프라인 구조**: 표시(DOM `<img>`, crossOrigin 없음)와 내보내기(canvas, `crossOrigin='anonymous'`)가 같은 사진을 **다른 URL·다른 CORS 모드**로 요청 → 브라우저 HTTP 캐시 키가 갈라져 저장 때 전량 재요청 → 프록시 18개 동시 폭주 → 일부 타임아웃(부분 실패). 게다가 DOM은 no-cors라 성공하므로 **v8.1의 깨진 사진 경고 3중 장치가 이 실패 모드에서 구조적으로 하나도 발화하지 않았다**(보드엔 18장 멀쩡, 시트에서만 "8장 빠졌어"). 부차 원인: 붙여넣은 임의 호스트 URL이 원본 그대로 저장돼 프록시 허용목록 밖 → 403 결정적 실패. 오너 결정 3건(P0 먼저 분리 배포·붙여넣는 순간 내 저장소로 복사·UI/UX는 콜라주+저장 흐름 집중)
- 구현 A(이미지 로드): `lib/imageSrc.ts` 신설 — `displaySrc()`가 보드 사진을 그리는 모든 곳(CollageBoard·MiniBoardPreview·/scenes 슬롯·Lightbox)과 캔버스 로더의 **단일 관문**, 원격 허용 호스트는 동일 출처 프록시 URL로 통일(캐시 엔트리 1개·taint 0·내보내기 시 네트워크 ≈0). `loadOne` 재작성(프록시 우선·crossOrigin 미설정으로 DOM과 조건 일치·프록시 장애 시에만 직행 폴백), src dedupe·동시성 6(`mapLimit`)·상위 데드라인 20s(무한 "만드는 중" 제거)·`skippedKeys` 반환. 프록시 route: 버킷 `'img'` 600/분·리다이렉트 목적지 3홉 재검증(구 follow는 오픈 프록시 성향)·`TransformStream` 실바이트 상한(구 content-length만 검사)·`public, immutable` CDN 캐시(구 `private`가 함수 폭주의 원인)·**실패 응답 `no-store`**. `lib/apiGuard` `rateLimited(req, max, bucket)` — 구 키가 IP뿐이라 사진 18장 로드가 `/api/story`(max 20) 한도를 먹던 잠재 장애(프록시를 주경로로 만들면 확정 장애가 될 뻔함). WallpaperSheet: 재진입 가드(buildingRef/pendingRef/runIdRef)·재시도 시 cache-bust·실패 사진의 **섹션 이름 표기**+`[다시 시도] [내 보드로 가져오기]`, itemsKey 변화로 재렌더(수입 직후 직접 호출은 낡은 props를 써서 방금 고친 사진이 또 빠졌음)
- 구현 B(정규화·복구): `app/api/image/fetch` 신설 — 임의 호스트 1회성 수입 전용(프록시와 분리: 프록시는 좁은 허용목록+immutable 1년이라 임의 호스트를 얹으면 캐시되는 오픈 이미지 프록시가 된다). 방어 = 동일 출처 게이트(sec-fetch-site→referer 폴백)·`imgfetch` 60/분·https+기본포트·**DNS 조회 후 모든 A/AAAA 공인 IP 검사**(10/8·172.16/12·192.168/16·127/8·169.254/16·100.64/10·fc00::/7·fe80::/10, `::ffff:` 언랩)·이름 블록(localhost·*.local·*.internal·metadata.google.internal)·리다이렉트 홉마다 재검증·content-type 화이트리스트(**SVG 금지**)·8MB·no-store. `lib/imagePick importRemoteImage`+`IMPORT_NOTICES`가 모든 붙여넣기의 단일 관문(핀 차단 흡수·**`data:`는 pass-through로 재압축 금지** — v81r1 C-3·v81r2 V-7c 정확 일치 계약), `/scenes`·`/collage` 호출부 교체(loadOne 프로브 제거 — 서버가 실제로 받아본 것이 더 강한 검증). `lib/imageNormalize.ts` 신설 — 마운트 일괄 변환·스키마 마이그레이션 **안 함**(첫 화면 멈춤·불필요 용량·부분 실패 시 재시도 불가), 실패가 확정된 두 지점(저장 시트·콜라주 깨진 사진 배너)에서만 온디맨드. 보드 스키마 v4 불변
- 구현 C(콜라주 컨트롤): 기기 사이즈 칩을 템플릿 탭 **바로 아래 전폭**으로 이동(구 v8.2는 폰 뷰 lg에서 보드 우측 20rem 레일 — 칩 7개가 안 들어가 잘림), lg `flex-wrap`(좁은 화면은 스크롤 행 유지 — wrap하면 3행이 되어 보드를 깎는다), 레일 폐지(칩이 빠지면 상주 콘텐츠 0), lg 헤더 1행 통합(뷰 탭을 제목 줄로 접어 ≈52px 회수 — AccountButton·뷰 탭은 DOM에 하나뿐이어야 aria strict 매칭이 산다), `--board-reserve` 소유자를 공용 래퍼로 이동(desktop lg 17rem·phone lg 10rem), 폰 뷰 lg 스테이지를 보드 폭에 맞춰 좁힘. **부수 발견 버그**: sticky 저장 바·ScrollRow 페이드의 그라디언트 끝점이 `to-transparent`(= 투명한 **검정**)라 중간에서 회색 얼룩 — 같은 색 알파 0으로 교체(v8.2부터의 결함), ScrollRow에 `fadeColor` prop
- 검증·배포: `verify-v87r1` 신설 41케이스(URL 동일성·동시성·데드라인·재진입·부분 실패·섹션명 / 프록시·fetch 보안 11 / 정규화·복구·만료 8 / 칩 배치·무스크롤 6) + `verify-v82r1` **V82-2b 계약 반전**('레일이 보드 우측'→'칩이 보드 위') → 전 스위트 PASS. ⚠️ 계약 균형: PC 뷰 무스크롤(1280×900)과 보드 폭 ≥1000px·18장 최소 셀 ≥100px가 동시에 성립해야 해서 lg 크롬을 ~100px 다이어트하고 reserve를 테스트로 역산했다(하드코딩 아님 — V87-4d/4e가 양방향 고정). ⚠️ route.fulfill 응답은 HTTP 캐시에 안 들어가 바이트 재사용은 관측 불가 → 계약의 핵심인 **URL 동일성**으로 단언

### 2026-08-01 (v8.6 — 진짜 충돌 때만 묻는 병합 판정·로그아웃 완료 화면, 프로덕션 배포 완료)
- 오너 피드백 2건(로그아웃 무변화·MergeSheet가 뜰 이유 없는데 뜸) → 탐색 2병렬로 원인 확정: decideMerge가 "양쪽 다 내용 있음"만 보고 무조건 ask(동등성·마지막 푸시 추적 부재) + "나중에 정할게" 리로드 시 조용 덮어쓰기 버그 + 시트 닫으면 탭 세션 내 푸시 영구 중단(서버 7.31 정체의 원인) / 로그아웃은 '/' 이동하지만 대시보드가 100% localStorage 렌더라 픽셀 동일. 오너 결정 3건(진짜 충돌만 ask·로그아웃 전용 화면·로컬 보드 항상 유지)
- 구현(3885a28): `lib/syncStamp.ts`(rev 카운터·modified-at·synced-local-rev/server-at, 보드 스키마 v4 불변)+PUT RETURNING updated_at(GET과 동일 epoch ms)+sync.ts 변환 후 rev 캡처·성공 시 스탬프 기록, decideMerge v2(순수 유지·옵셔널 4번째 인자 — v81r1 D-1 tsc 단독 컴파일 하위호환), saveLastVisit skipRevBump(마운트마다 도는 휘발 저장이 "로컬 무변경" 분기를 오염시키던 결함 — 설계 검토 중 발견), vb-merge-deferred 보류 지속, runSync 재큐잉·5s 1회 재시도·pagehide/visibilitychange 디바운스 플러시(keepalive), `/logged-out` 신설+AccountButton 세션 스탬프 정리·signOut 실패에도 이동·계정 삭제 시 동기화 스탬프 정리
- 검증·배포: verify-v86r1 신설 16케이스(조용 채택 3경로·진짜 충돌·보류 무덮어쓰기·스탬프 기록·플러시·로그아웃 6단언·decideMerge 단위 5분기 — 서버 보드 키 셔플로 jsonb 순서 무관 검증) → 전 스위트 28/28(기하 453 포함)+r2a·v81r1 무수정 통과 → master 푸시+`npx vercel --prod`+prod v86r1 16/16+스모크 20/20+비로그인 PUT 401. ⚠️ 스탬프 기록 이전의 기존 서버 보드는 최초 로그인 1회 ask가 의도

### 2026-07-31 (v8.5 — 완주 피드백 리플래닝: 언어 게이트·/diary 인라인 다듬기·대시보드 가치·콜라주 배치, 프로덕션 배포 완료)
- 오너 피드백 10건(일본어 혼입·다듬기 플로우·/diary 구조·대시보드 카피/가치·이야기 블록·미니멀 배열·반자동 리사이즈·날짜 변경·아이콘·MergeSheet 불명) → 탐색 3병렬+설계 에이전트로 v8.5 플랜 승인. 오너 확정 3건: 미니멀=큰/작은 혼합 그리드(단일 히어로 아님), 날짜=보드 1개 변경, 아이콘=도토리 모티프
- 구현(87efbdb): 공용 `lib/storyGate` 게이트(외국 문자 검출·재생성 후 재검증·기계 제거 폴백, summarize도 freeChat 이전)+STORY_PROMPT_VERSION 3, /diary 인라인 다듬기(보드 무제한·섹션 2캡 공유, `lib/storyClient` 공용화로 /finish도 단일 경로), 헤더 승격·날짜 편집·탭 랩·부록 삭제·스크롤 리셋, 대시보드 카피/walkCaption 정리+북극성·D-day, /collage 블록 삭제→링크, MergeSheet 요약·시각, 미니멀 밴드 타일링(C 후보 탐색·스팬≤4 락스텝·n=1/2 특례), 모자이크 비정사각 스냅(캡 1..4 통일)+고스트 프리뷰+자유 배치 안내
- 검증·배포: verify-v85r1 신설 32·기하 453(§8/§9 신 계약 — 고아 판정은 콘텐츠 폭 기준으로 교정)·구 스위트 6종 계약 갱신 → 로컬 전 스위트+스토리 9/9 PASS → master 푸시+`npx vercel --prod`+prod 스모크 20/20+33/33+28/28+32/32+스토리 9/9. ⚠️ groq-sdk "미사용" 오판(glob 필터 위양성)으로 uninstall→tsc 확증에서 발각·원복(교훈 기록)

### 2026-07-31 (v8.4 — 배경화면 내보내기 복구·콜라주 배열·완주 카피·/diary 탭·스토리 프롬프트 고도화, 프로덕션 배포 완료)
- 오너 피드백 6건 → 탐색 3병렬로 원인 전수 확정: 사진 누락 = 로그인 후 Blob URL의 캔버스 CORS 로드 실패 + 프록시 허용 호스트에 자기 Blob 호스트 부재(403) / 숲 "다 안 보임" = pre-v8.0 겹침 레이아웃이 edited 취급 보존 / 미니멀 고아 행 = cols 탐색이 셀 크기만 최적화. 오너 결정 2건(diary 탭 = 전체+섹션 6탭, 기존 이야기 = 재작성 넛지)
- 구현(5402772): 프록시 Blob 호스트 허용+loadOne 타임아웃(10s/15s)+"다시 시도", showSaveFilePicker 저장 위치 픽커(클릭 첫 await 활성화·폴백 유지·미리보기 캔버스 재사용), `isLayoutBroken` 숲 레거시 가드(쌍 겹침>50%·경계 이탈, edited:true 불변), `layoutBalancedGrid` 균형 행(행당 차 ≤1 — seedMinimal·reflowLayout 공유), 완주 카피 "완성해도 끝은 아니야"+일기 버튼 승격, /diary 7탭, 스토리 프롬프트 v2(사람 먼저·마찰 1개·오글 금지 열거·하루의 결 랜덤·문장 리듬)+`hasCliche` 게이트 공용화+Groq 폴백 70B(실호출 검증)+storyPromptVersion 넛지(대시보드·/diary, 닫기 1회)
- 검증·배포: verify-collage-layout 352(균형행 153·reflow 라운드트립·숲 가드 블록 추가)·verify-v84r1 신설 28(픽커 스텁·프록시 계약 포함)·갱신 5스위트(v83r1 V83-4c 교체·r2b 픽커 스텁 — 헤드리스 픽커 자동 AbortError로 저장이 '취소' 처리되는 함정·v612~614·smoke-v80-story S8 클리셰) → 로컬 25스위트 전건 PASS → master 푸시+`npx vercel --prod`+prod 스모크 20/20+v83r1 33/33+v84r1 28/28+스토리 8/8. ⚠️ PS5.1 Get/Set-Content 라운드트립으로 story route 한글 전파 손상 → git checkout 원복+Write 재작성으로 복구(교훈 기록)

### 2026-07-31 (v8.3 — 완주 경험·재방문 로그인: /diary 통합·완주 정리·온보딩 로그인 진입·완주 연출, 프로덕션 배포 완료)
- 오너 피드백 5건+미래 일기 3안 검토 → 기획서 `docs/완주경험-리플래닝-v83.md` + 플랜 모드 승인(오너 결정 3건: ③ 통합 뷰·완료형+편집 숨김·참나무 승격+파티클). 로그인 복원 인프라는 AccountFlow 전역 병합이 이미 동작 확인 — 온보딩 셸 트리거 링크만 추가하면 되는 구조였음
- 구현(0dc8ce0): `/diary` 신설(하루 이야기 대표+섹션 일기 부록·부분 진행 안내·이야기 다듬기→/finish)+대시보드 일기 진입점 1원화(DiarySheet 폐기), 완주 시 📷 행·연도 편집 숨김+"그린 보드야", OnboardingShell "Google로 이어가기"(callbackUrl '/'→루트 배분)+choice 캡션, WalkPathMap 다 자란 참나무(글로우·탭→/collage·미완주 "참나무 언덕" 불변)+gradientDoneCss+첫 완주 잎 파티클(finishCelebrated 스탬프·leafFall keyframes reduced-motion 가드), api/story 한 문장 변주 마무리 지시
- 검증·배포: verify-v83r1 신설 32케이스(완주/미완주 양분기·파티클 스탬프 E2E) + v79 케이스5 /diary 재작성·v81r1 B-1b·smoke-v81-prod S3b 갱신(완주 시드 finishCelebrated:true 관례 신설) + 로컬 22스위트 전건 PASS + LLM 스모크 7/7(S7 영역명 소제목 가드 신규) → master 푸시+`npx vercel --prod`+prod 스모크 20/20+prod v83r1 32/32. 잔여: 오너 육안 3건(연출·/diary·실계정 복원)

### 2026-07-31 (v8.2 — 콜라주 뷰 분리 복원·이미지 극대화·리사이즈 리플로우·라이트박스, 프로덕션 배포 완료)
- 오너 피드백(나란히 레이아웃으로 이미지 과소·양옆 여백·경직된 편집·확대 부재) → 탐색 2병렬로 원인 확정(grid-cols-2 절반 폭 + `100dvh-19rem` 높이 예산 + PC cols+2 공식의 3중 축소) 후 디자인 스킬 3종 로드하여 플랜 수립·승인(floating-swinging-russell)
- Slice 1~4 구현(9053f96): 나란히 철회→2탭 복원+저장 버튼 sticky 바+`--board-reserve` 변수(13rem/lg 10rem)+폰 뷰 lg `grid-cols-[1fr_20rem]` 레일+프리셋 캡션→선택 칩 서브텍스트(aria-hidden으로 radio 이름 계약 보존) / 시드 cols 최적 탐색(모자이크 2..9·미니멀 1..9, 최종 셀 폭 최대 — n≤2 특례 불요)+가로형 titleBottom 0.16+**k-축소 갭 제외 교정**(availH/neededH 방식은 (1-k)×갭 하단 넘침 — 리플로우 검증이 노출한 잠재 버그) / `inferGridSpans`+`reflowLayout`(읽기 순서 재패킹·z 보존·자유 배치 존중·숲 제외)+`.collage-item-settle` 220ms(reduced-motion 가드) / 공용 `components/Lightbox.tsx`(scenes 추출·contain variant)+좌표 히트테스트(회전은 픽셀 공간 역회전)+'✎ 탭해서 편집' 버튼 승격
- 검증·배포: verify-collage-layout 186(reflow 36 추가)·verify-v82r1 신설 17·verify-v81r2 전면 재작성 33·회귀 12스위트 전건 PASS(v73r1·v72r1 캡션 단언 갱신)·스크린샷 4종 육안 → master 푸시+`npx vercel --prod`+prod 스모크 20/20+prod v82r1 17/17

### 2026-07-30 (v8.1 — 수정 플로우 정상화·완주 후 상태·이미지 신뢰성·콜라주 나란히+인라인 편집, 프로덕션 배포 완료)
- 오너 실기기 피드백 5건 → 프로덕션 localStorage 실데이터+탐색 3병렬로 원인 전수 확정: 콜라주 빈 칸=핀터레스트 페이지 주소 무검증 저장(로드 테스트 9건 중 1 FAIL 실측), "완성하러 가기" 잔존=완주 상태 분기 부재(futureDayStory 858자 확인), 무변경 '수정' 회귀=markSectionTextComplete 무조건 강등+LLM 재검증 비결정 판정+무조건 다운스트림 배너 3중 결함, 모바일 우선=/finish ?view=phone 딥링크
- 구현은 Ultraplan 클라우드 세션(브랜치 claude/v81-edit-flow-collage 커밋 2개) → 로컬 ff-merge 인수: Slice 1(강등 가드·dirty 게이트·변경 시 "다시 쓸까/그대로 둘까" 선택지·완주 CTA·URL 게이트 loadOne 프로브·merge 유실 가드) + Slice 2(lg+ 나란히 실렌더·좁은 화면 기본 phone·편집 모드 섹션 색 테두리+라벨 칩·사진 바꾸기/지우기 인라인(lib/photoSlots.ts 공용)·깨진 사진 ⚠️+저장 전 경고+시트 skip 안내)
- 검증·배포: 신설 verify-v81r1 35/35·v81r2 32/32 + 갱신 구 스위트 5종 128/128 + check-wallpaper PASS(클라우드 샌드박스 실패 6건은 로컬에서 전부 통과 — 환경 실패 확인) → master 푸시 + `npx vercel --prod` + prod 스모크 19/19. 오너 복구 잔여: 섹션1 재완료(저장 1회)·깨진 사진 교체(콜라주 편집)

### 2026-07-30 (v8.0 — 완주 피드백 14건: 가이드 단순화·카피 정합·최종 일기 품질·콜라주 자동배치/회전, 프로덕션 배포 완료)
- 오너 첫 6섹션 완주 피드백 14건(캡처 13장) → 탐색 3병렬로 원인 전수 확정(칩·예시 전부 고정 템플릿 / 최종 일기만 Llama-8B 저품질 경로 + 섹션 일기 미사용 / 숲 슬롯 의도적 겹침 + z=섹션순 + 인덱스 reconcile 버그) + 오너 결정 3건(최종 일기=고품질 재생성, 가이드=예시 1겹+도움 1버튼, 기본 레이아웃=모자이크)
- R1: /scene 안내 5겹→2겹(dead data였던 sceneStep.helpQuestions 18개 연결), questions.ts 정합 감사(S2 keyword 질문 직접화·S5 예시 '사이' 서술·S4 keyword 3종 재작성·S6 Q1 이중질문 교체·Q3 감각 중복 해소·S3 feeling 단일화·placeholder 통일), SlotValue 공용 불릿 렌더러 3곳, /api/story 전면 재작성(freeChat 경유·섹션 일기 재료·통합판 시스템 프롬프트·존댓말 정규식 게이트 1회 재생성 `lib/honorific.ts`), /finish renderStory + CTA 통합(구 2버튼이 같은 /collage 탭 차이)
- R2: 숲 예약셀 지터 그리드(`polaroidReserveRect` 연도 카드 회피, 무겹침, landscape 축소 폐기), `CollageLayout.edited` 플래그 + identity reconcile(`placeNewItems` 최소 겹침 스캔 — 새 사진이 기존 밑에 깔리던 버그 제거), 회전 핸들 ↻(0° 스냅·±30°·회전 bbox 클램프)+사진 탭 '맨 뒤로/바로 세우기', 스티커 회전 원점 DOM=canvas 정렬, DEFAULT_TEMPLATE=mosaic 단일 소스, 모자이크·미니멀 수직 센터링
- 검증·배포: 카피 계약 4스위트 갱신(v7r2·v73r1·v74r1·v76r1) 후 21스위트 468 PASS, 신규 `scripts/verify-collage-layout.js` 150 PASS(겹침≤20%·카드 무가림·센터링·reconcile 불변성), 회전 인터랙션 8 PASS, LLM 실호출 `.claude/smoke-v80-story.mjs` 로컬 6/6·프로덕션 6/6 → `npx vercel --prod` + 프로덕션 스모크 15/15. lint 49건은 기준선 동일(신규 0)

### 2026-07-27 (R2-2 게스트 로그인 유도 3종 — 소프트 게이트·배너·배경화면 보조, 프로덕션 배포 완료)
- 승인 플랜(rustling-pudding) Task 1~6 그대로 구현(9e1a278): `components/LoginNudgeSheet.tsx` 신규(첫 사진 이후 첫 대시보드 방문 시 1회, 클릭·닫기 모두 loginNudgeSeen 스탬프 — OAuth 취소 복귀 재노출 방지, 인트로 시트 우선), 대시보드 1줄 배너(🔒 + 7일 쿨다운 — 게이트 닫는 순간 시작, ≤1.2뷰포트 예산 예외 각주), WallpaperSheet 저장 성공('cancelled' 제외) 후 "이 보드, 계속 간직할래?" 1줄. BoardData additive 2필드(스키마 v4 유지)+storage 2함수. 주 버튼 "Google로 로그인해두기"로 r2a 'Google로 로그인' exact strict 충돌 원천 차단(BN-1c로 계약 명문화)
- 검증·배포: verify-r2b 신설 18케이스 + 기존 4종 무회귀(r2a 19·v71r4 24·v78r1 26·v7r1 31 — v71r4/v78r1 doneBoard 시드에 게이트 억제 선주입, r2a 무수정 통과) → `npx vercel --prod` → 프로덕션 Playwright 실렌더 스모크 5/5. 유일한 플랜 편차: /collage 기본 view=desktop이라 검증만 `?view=phone` 진입(앱 무수정)
- 프로덕션 스모크 1차 FAIL 1건은 앱 아닌 테스트 버그 — addInitScript 시드에 `if (!localStorage.getItem)` 가드 누락으로 reload가 스탬프를 덮어씀(교훈 기록)

### 2026-07-27 (R2-1 프로덕션 배포 완결 + 온보딩3 위계 + R2-2 플래닝)
- "로그인이 프로덕션에 안 보인다" 신고 원인 확정 = **미배포**(R2-1 13커밋이 로컬에만) + Task 0 리소스 미프로비저닝 → Google OAuth 클라이언트(helen.easytask)·Neon(스키마 `db-init-r2.mjs`)·Blob 생성, `.env.local` 5종 + Vercel prod env 3종 등록(파일 리다이렉트, Ascii 인코딩으로 BOM 회피) → `npx vercel --prod` 배포·푸시. 실가입 1건 Neon SELECT로 확인(marketing_consent=true, 보드 업서트 동작)
- 온보딩3 상태 연동 위계(ff8ce8c): 유도 카피 "슬라이더를 끝까지 밀어서/선명하게 만들어봐" text-body semibold 인디고 2줄 승격, CTA "비전보드 시작하기" 대기(#E5E3DF, 클릭 가능)→완료 시 검정+ctaPop. 회귀 68케이스+스타일 스팟 12+프로덕션 실렌더 9/9 PASS. ⚠️ /onboarding·/dashboard SSR HTML은 빈 body — raw HTML 스모크 무효(Playwright 실렌더로)
- R2-2 플래닝 확정(사용자 승인): 소프트 게이트는 **첫 사진 이후 첫 대시보드 방문 시** 노출(모든 사진 유입 경로 커버), BoardData additive 필드 2개(loginNudgeSeen·loginBannerDismissedAt), 배너 7일 간격, C 배경화면 1줄 — 플랜: `~/.claude/plans/https-vision-board-web-vercel-app-onboar-rustling-pudding.md`, 구현은 새 세션

### 2026-07-24 (v7.8 R1 — "첫 보드" 마일스톤: 3섹션 조기 개방·재작성 넛지·6/6 상향 목표화, 프로덕션 배포 완료)
- 오너 확정 2건: 임계값 **3섹션**(인트로 "먼저 마음 가는 세 곳부터"와 정합), 범위 **코어만**("v7.8 후보" 2건은 다음 라운드) → `docs/첫보드-마일스톤-v78.md` 신설
- `lib/milestone.ts` 단일 소스: FIRST_BOARD_THRESHOLD=3 + isStoryStale(재작성 넛지 판정 — storyWrittenAtCount 없는 구 데이터는 항상 false, 업데이트만으로 넛지 안 뜨게). saveFutureDayStory가 저장 시점 completed 수 스탬프(생성·수동 수정 모두 — 어느 쪽이든 최신본이라 넛지 접힘)
- 게이트 완화(리플래닝-v74 §2 구현 메모 그대로): 대시보드 "🌳 첫 보드가 열렸어" CTA(allTextDone이면 /review CTA 우선 — 중첩 방지), 콜라주 완성 CTA ===6→>=3(6/6 라벨 "내 비전보드 완성하기" 불변 — v75r1 계약), /review 게이트 유지. 완료 시트 3번째 완성 순간 "🎉 첫 보드가 열렸어" CTA(보상 회수 지점), /finish 부분 상태 카피 + api/story "6개 섹션(나·건강·…)" 하드코딩 제거
- 검증·배포: verify-v78r1 신설 26케이스(임계값 경계 2/3/6·스탬프 E2E(/api/story 모킹)·구 데이터 가드·중첩 방지) + 기존 16스위트 회귀 = **17스위트 412케이스 전체 PASS** → `npx vercel --prod` + 프로덕션 스모크 13/13(9경로 200 + 조기 개방 CTA·재작성 넛지 실렌더, `.claude/smoke-v78.mjs`)

### 2026-07-24 (v7.7 R1 — 실사용 피드백 라운드 2: 미니보드 숲 정합·아이콘 재매핑·경로 실선·예시 5세트·토리 코치마크, 프로덕션 배포 완료)
- 오너 피드백 9항목 → 기획 정리(`docs/피드백-라운드-v77.md` 신설) + 오너 결정 4건: ① 완료 시트 구버전 프리뷰는 삭제 아닌 **수정**(MiniBoardPreview가 v7.6 부분 마이그레이션 — MiniPolaroid 흰 폴라로이드→CollageBoard와 같은 프레임리스 FOREST.card 타일, 공용 5곳 일괄 반영), ② 유연한 경로는 **경로 고정+완료 구간 실선** 채택(buildSegments 세그먼트 분해, visitedAt 대공사는 백로그), ③ 질문 예시만 3→5세트(씬 일기 [산문,불릿] 2세트는 형식 시연이라 유지), ④ 토리 가이드는 기존 /collage 코치마크에 **프로필 발화자 승격**(스포트라이트 투어 백로그)
- 상태 아이콘 재매핑: `lib/stationStatus.ts` 신설(단일 소스) — in_progress=💬 답하는 중(카메라 오노출 해소), text_complete=📷 사진 차례, ✍️ 퇴역, 사진만(not_started+photo)=📷 유지(퇴행 가드). "아이콘=다음 행동" 원칙. 스테이션 button에 title 툴팁(데스크톱 호버, aria-label 계약 불변). 미니보드 뱃지 동일 정합
- 예시 5세트: 신규 페르소나 축 2종 — 전환기형(이직·이사·복직, 시점 마일스톤 명사)·관계공동체형('누구와' 주어+빈도 숫자), 24질문×2세트=48카피(`lib/questions.ts` 단독 수정, InlineInput `% length` 로직 무변경)
- 넛지: walkCaption "서두르지 않아도 돼" — 최초 긴 버전이 v71r3 R3-1b(대시보드 ≤1.2뷰포트) FAIL(1.21) → 카피 축소로 예산 준수. "첫 보드" 3섹션 마일스톤은 v7.8 단독 라운드로 앞당김 확정(리플래닝-v74 §5 분리)
- 검증·배포: verify-v77r1 신설(24케이스: 숲 타일·💬/📷·title·실선/점선·5세트 순환·코치마크) + v75r1 이모지 카운트·v76r1 5회 순환 갱신 → **16스위트 전체 PASS** + 스크린샷 3장 육안 확인 + `npx vercel deploy --prod` + 프로덕션 스모크 18/18(9경로 200+신규 요소 시드 실렌더)

### 2026-07-23 (v7.6 R1 — 실사용 피드백 8건: 숲 테마·시작 어포던스·예시 로테이션·씬 브리지, 프로덕션 배포 완료)
- 오너 피드백 라운드: ① "검은 배경" 정체는 다크 패널 3곳(#2D2B29) → **딥 포레스트 그라디언트 채택**(사용자 결정, #1F2E22→#2A3D2E, `lib/colors.ts FOREST` 단일 소스, DOM/canvas 락스텝, 배경화면 픽셀 샘플 검증) + 완료 스테이션 🌿 수풀, ② 흰 폴라로이드 프레임 제거(사진만+라운드·그림자, 폭 +8%, 템플릿 라벨 '폴라로이드'→'숲'·id는 localStorage 키라 유지), ③ '나' 노드 시작 어포던스(glowRing 확산+정적 링 2겹, 첫 방문 "여기서 시작 ▾" — 사진 포함 진행 시 소멸), ④ 도토리 채팅 opacity-60 딤 제거
- 예시 전면 개편: `example: string`→`examples: string[]` 강제 rename(tsc로 소비처 전수 열거) — 질문당 3세트(결핍·유지·성장 페르소나, 구체 명사/숫자≥1, 오너 실답변의 구체성 수준으로 신규 작성), want 3줄 멀티라인+"떠오르는 만큼 여러 개 적어줘", 형식형 플레이스홀더, InlineInput "다른 예시 보기 ↻" 순환(세트 0 시작 — 테스트 결정성)+rows prop
- 씬 브리지(사용자 명세: 안내가 씬1 안에 나온 뒤 전환): 리뷰 검증 통과 → 토리 버블("'키워드'가 이뤄진 N년의 하루… '미래의 하루 일기'로 다듬어줄게", josaOnly 조사 처리)+CTA "미래의 하루 그려보기 →"가 페이지 전환. /scene 쿠션은 "질문은 끝났어"→재인사로 교체, 가이드 카드 3행("문장으로 써도, 장면 단어만 나열해도 좋아"), 씬 예시 산문/불릿 2세트 로테이션
- 마이크로 카피(행동→기대값): 이름 힌트·5분 기대값·인트로 최종 보상(참나무 언덕 배경화면)·pathSheet 선택지별 결과("질문 4개에 답하면 → 미래의 하루 일기"). 스티커 프리셋 8→18종(이모지는 chip/script만 — outline strokeText가 이모지 외곽선을 깨뜨림)
- **로그인 확정**: 이메일+비번 대안 검토 → 기각(비밀번호 재설정용 메일 발송 인프라·해시 저장이 추가 필요, 마케팅 동의 구조는 인증 방식 무관) → Google SSO 유지, 기획서 v7.6 재확인 블록 기록
- 검증·배포: verify-v76r1 신설 43케이스 + 구 4스위트 갱신('폴라로이드'→'숲' 3곳 `.first()`, v73r1 쿠션 단언) → **15스위트 362/362 PASS** + 배경화면 캔버스 픽셀 샘플 + `npx vercel --prod` + 프로덕션 스모크 15/15(콜라주 템플릿 셀렉터는 사진 있어야 노출 — 빈 시드 위양성 1건 교정)

### 2026-07-23 (v7.5 R1 — 산책길 대시보드 + 콜라주 2탭 + 회원가입 기획서 개정, 프로덕션 배포 완료)
- 실사용 피드백 라운드: ① 대시보드 미니보드(첫 사진 대표)가 공간 대비 정보 가치 낮음 → **산책길 지도 채택**(사용자 결정, 기존 산책길 기획서 R1 범위), ② "1칸이 채워졌어" 캡션 제거, ③ CTA를 타이틀 바로 아래로, ④ 콜라주 보드 탭 제거, ⑤ Google 로그인 유도 구조 재결정
- `components/WalkPathMap.tsx` 신설: % 좌표 앵커 8개 단일 소스 → SVG 점선 bezier 경로 + absolute 스테이션 버튼(44px, 상태 이모지 🌰/📷/✍️/🌳+완료 칩, 사진 미표시), 토리 마커 = 추천 스테이션. aria-label 계약 유지로 구 셀 라우팅 케이스 무수정 통과. 진행 카피 4분기(시작 전/산책 시작/N/6 스테이션/길 끝 도착, 분자=completed) — 대시보드 높이 ≤1.2뷰포트 유지(R3-1b 무완화 통과)
- 대시보드 재배치: 추천 카드→리뷰 버튼→내 비전보드 보기→사진부터 CTA 묶음이 타이틀 직하, 진행바→산책길 지도→연도 행 순. DashboardIntroSheet "6칸짜리 정원"→"6개 스테이션이 있는 산책길"("세 칸부터"→"세 곳부터")
- 콜라주 2탭: CollageView에서 board 제거, BOARD_EXPORT_PRESET·"이미지로 저장"·부분완성 안내 삭제, `?view=board`→desktop 정규화, 완성 CTA·미래의 하루 이야기 블록을 기기 뷰 공통 노출로 게이트 해제(collageLayouts 데이터는 스키마 보존)
- 회원가입 기획서 v7.5 개정(구현은 R2): 유도 = 온보딩 종료 "Google로 시작/게스트로 시작" 선택 화면(게스트 유의 카피 — '캐시' 어휘 금지, "이 기기 브라우저에만 저장돼") + 첫 사진 직후 소프트 게이트 주 유도(C 배경화면 저장은 보조 강등), 동의 2종(필수 개인정보 + 선택 마케팅 "마인드/자기발견/자기성장/코칭" — 미체크 가입 차단 불가, users.marketing_consent), §9 운영 가이드(Neon SQL 가입자·활동 조회, Web Analytics, track은 Pro 전용)
- 검증: verify-v75r1 신설 27케이스 + 구 스위트 7종 갱신(v73r1 보드 탭→부재·정규화, v74r1 카피, v7r5 산책길·사진 부재, v71r3 참나무 언덕, v72r1 🌱→🌰·PC 복귀, v7r1/v71r1 인트로 카피, v618b 아카이브) → **14스위트 319/319 PASS**. 신규 발견: 산책길 '완료' 칩이 연도 편집 '완료' 버튼과 strict 다중 매칭 → role 셀렉터로 교정. hydration #418은 기존 전역 패턴
- 배포: 사용자 승인 후 `npx vercel --prod` — Ready 확인, 10경로 200 + /moment 404, 프로덕션 번들 grep(스테이션 카피·참나무 언덕·산책길 인트로 존재, "1칸만 있어도" 부재)

### 2026-07-23 (v7.4 R1 — 전면 리뷰 리플래닝 + 6섹션 허들 해체 R1 + LLM 무료화, 프로덕션 배포)
- 전면 리뷰 4문항(가입·여정·Pinterest·일기) + 사용자 추가 요청 "6섹션이 최선인가" 진단 → `docs/리플래닝-v74.md` 확정: 6섹션 프레임 유지, 6/6 허들의 실체는 CTA 3곳+카피(시스템은 이미 부분 상태 관용), 3단 해체(R1 카피 → R2 3섹션 "첫 보드" 마일스톤 → R3 리빌 넛지), 선택형 섹션 폐기(회피 영역=발견 가치 최대), Pinterest 폐기·회원가입 기획서 R2 채택
- 구조 카피(R1 몫): 개화("피어나/피었어") 제거→중립 진행, 초반 분모 6 완화("N칸 채워짐"), 부분 가치("1칸만 있어도 네 보드야"+보드 버튼 배경화면 부제+collage 부분 상태 1줄), 인트로 시트 코어 경로("먼저 마음 가는 세 칸부터")
- 퀵윈: 일기 재생성 합산 2회 제한→직접 수정 유도, "나중에 답할게요" 슬롯 유예(keyword 불가, 완료 판정 "keyword 필수+답변/유예", 리뷰·/review 회수 동선), pathSheet 프리하이라이트+3연속 생략, 다중 파일 업로드, 완료 시트 "오늘은 여기까지도 충분해", scene 가이드 접힘, 질문 정비 4건(S3 current 현재 인식·S6/S2 feeling 각도·S4/S5 양방향)
- 온보딩 몰입(스킵 없이): Step1 실시간 호명 미리보기, Step2 도토리 심기 인터랙션(CSS 키프레임 낙하+새싹), Step3 막연→선명 슬라이더 직접 조작
- LLM 0원화: lib/llm.ts(Gemini flash 무료 티어 1차→Groq 2차), OpenAI 라우트 4개 전환, @anthropic-ai/sdk·openai 제거. 고정 모델명(gemini-2.5-*)이 "no longer available to new users" 404로 부패 → `gemini-flash(-lite)-latest` 별칭 채택. Vercel env는 파이프 stdin이 빈 값으로 등록되는 함정 → 개행 포함 파일 리다이렉트로 재등록+재배포
- 검증·배포: 사전 v7.4 감사 변경분 별도 커밋(b67d75b) → R1 커밋(cbc733a) → verify-v74r1 신설(32케이스)+구 스위트 5종 신 UX 갱신 → 13스위트 290/290 PASS, 일기 3편 기계 검증(450~650자·볼드 1곳), 프로덕션 스모크(11경로 200·번들 카피 grep·Gemini 시그니처 실측)
- 전역 규칙 승급 2건(HITL 승인): Gemini latest 별칭 규칙 + Vercel env 파이프 함정 → `~/.claude/CLAUDE.md` (LESSONS에 승급 표기)
- 온보딩: 스텝1 누락 문장("우리가 함께 비전보드를 만들어 갈 거야") 추가 — 프로덕션 번들 grep으로 '배포 문제 아님·카피 부재'로 판별. 스텝2 도토리 문안 반말 교체 + CTA "그래, 함께 해보자!"
- 대시보드: "질문 없이, 사진부터 담아볼래?" 시트(섹션별 /scenes 직행), 연도 캡션+스테퍼 편집. 하드코딩 "3년 뒤" → targetDate 파생(scene 쿠션·review·finish)
- collage 재구성: 기본 뷰 PC, 사이즈 칩 상시 노출('사이즈 바꾸기' 패널 삭제·프리셋 자동 시드), 보드 뷰 이미지 저장(4:5), ?view= URL 동기화(?device= 호환)
- 일기 프롬프트 자연화(아크·체크리스트 제거, 구체성 규칙, 450-650자·temp 0.9 — 볼드 1곳 불변), scene 작성 가이드 카드 + sceneStep.example 6종 연결형 예문
- scenes: "직접 사진 올리기" 리네임, URL 입력 ① 토글 이동, ScrollRow(페이드+셰브런)로 갤러리 '성취·여유' 잘림 해결
- @vercel/analytics 설치 + track 5종, 기획서 3건(docs/ 산책길·회원가입·프로세스 단순화 — 구현 보류)
- verify-v73r1 신설(39케이스) + 구 스위트 9종 갱신 → 12스위트 256/256 PASS, `npx vercel --prod` 배포 + 11경로 200 + 번들 문자열 스모크

### 2026-07-08 (v7.2 정원 맵 + collage 한 화면 통합 + 카피 정비, 푸시·프로덕션 배포)
- 중단된 세션 재개: 커밋된 10태스크 플랜(docs/superpowers/plans/2026-07-08-v72-*) 기준 Task 4~10 실행 — Task 4는 중단 시점의 미커밋 diff가 플랜 명세와 1:1 일치함을 확인 후 그대로 커밋
- 카피: 온보딩 스텝1 간결화(T1, 이전 세션), 섹션1 '원하는 내 모습'(T2), scene "사진 담으러 가기"·"다시 써줘", section "미래의 하루 그려보기"(T4)
- 정원 맵(T5·T6·T7): 미니보드 씨앗🌱/새싹🌿·뱃지 3종(✓/✍️/📷)·토리 마커, 정원 캡션·추천 카드 문장형·'내 비전보드 보기' 단일 버튼(📱/🖥️ 퀵 버튼·'그냥 보드로 볼래?' 삭제), 인트로 시트 진행 방식 안내
- collage 통합(T8): choose 뷰 제거 → view 3값+sizePanelOpen 상태머신, [보드|폰|PC] 토글, 인라인 사이즈 피커+'사이즈 바꾸기' 칩, /finish 폰 버튼 ?device=phone 딥링크, 코치마크 전 뷰 노출
- 검증(T9): verify-v72r1 신설(26케이스) + 구 스위트 갱신 — 플랜에 없던 깨짐 3건 발견·수정(구 인트로 카피 단언 5곳, v71r4 추천 문장형, v7r5의 미존재 프리셋 id 'iphone-pro' 시드) → 10스위트 207/207 PASS
- 배포(T10): master 푸시 + `npx vercel --prod` → 9경로 200 + /moment 404, 프로덕션 Playwright로 새 카피·토글·정원 캡션 5체크 OK

### 2026-07-07 (v7.1 피드백 리플래닝 4라운드 + 사전 정리, 푸시·프로덕션 배포)
- 사전 정리: /moment·/board 스텁 철거+slots/SlotId 타입 제거(v4 백필은 로컬 캐스트 유지), 큐레이션 80→65장 육안 선별(어두운 실루엣·동일 촬영 중복·텍스트 노출 컷, 다양화 쿼리 8장 보충, 데드링크 65/65 통과)
- R1 온보딩: 스텝1 "그다음엔 우리 같이 비전보드를 만들 거야" 여정 예고+"너 그거 아니?" 말 걸기 톤, 스텝3 자동 교차 카드→흑백/컬러 위아래 동시 대비(CompareStack)
- R2 버그: 추천 사진 재탭=담기 해제 — uploadedImageSources 출처 병렬 기록, 갤러리 2종 토글화, 슬롯 ×와 양방향 동기화
- R3 대시보드=미니보드 허브: 섹션 카드 6장 제거→셀 탭 내비(✓배지·next 링), 추천 카드 1장, 📱/🖥️ 배경화면 퀵 버튼(/collage?device= 딥링크)
- R4 사진 먼저: 질문 강제 해제(양경로 시트·질문 페이지 링크), 넛지 배너/저장 직후 시트(1회 dismiss 영속), AI 힌트 게이트, 추천 우선순위(열린 고리 최우선), 48h 복귀 인사
- Ultraplan 클라우드 세션으로 플랜 정제 후 로컬 구현. verify-v71r1~r4 신설(73케이스)+구 스위트 4개 신 UX 갱신 → 총 197/197 PASS, `npx vercel --prod` 배포

### 2026-06-12 (v6.17 피드백 4개 영역 개선, 푸시·프로덕션 배포)
- 조사 버그 수정: `lib/josa.ts` 통합 유틸 신규 — 온보딩 `${name}라는` 하드코딩("헬렌라는")을 이라는/라는 분기로, getNameSuffix 대체·getEunga 데드코드 삭제 (단위 16건+렌더 검증)
- 색상: 대시보드 상태 뱃지 뉴트럴화+완성만 잉크 솔리드(보라 카드×초록 뱃지 충돌 해소), 온보딩 Act4 카드 0색·Act5 도트 문법(무지개화 제거, /board와 문법 통일)
- /board 간격: grid-rows-3 stretch가 잉여 높이를 행 안에 분산시키던 원인 제거 — auto-rows-min content-center
- /collage 개편: 1회 코치마크+편집 칩+탭 스와치, 폰/PC 미리보기 토글(WallpaperPreview+useWallpaperPreview), 기기 프리셋 11종(renderForPreset cover-crop), Unsplash 추천(ImageSuggestions, 모킹 E2E 검증·키 없으면 조용한 숨김)
- Playwright 22건+조사 16건 ALL PASS → 커밋 6639fbf, `npx vercel --prod` 배포, 4경로 200

### 2026-06-12 (v6.16 Act 4 간격 확대 + 최초 비비드 팔레트 복원, 푸시·프로덕션 배포)
- 온보딩 Act 4 간격 확대: 이미지 카드↔핵심 메시지 8→20px, 힘 메시지↔좋은 이유 12→24px — flex-1 이미지가 흡수해 무스크롤 유지, 375×667은 높이 미디어쿼리 분기(간격 한 단계+패딩 회수)
- 팔레트를 최초 Stage 1 커밋(59fee78) 비비드 톤으로 복원(파스텔 폐기, 사용자 요청) — colors.ts 단일 소스 1파일 수정
- `scripts/verify-v616.js` 신규(3뷰포트 무스크롤+간격 실측) ALL OK → 커밋 0574263 푸시 + `npx vercel --prod` 배포, 3경로 200

### 2026-06-12 (v6.15 파스텔 복원 + 전체 UI/UX 개편, 푸시·프로덕션 배포)
- 팔레트 v6.13 파스텔 복원(사용자 원복 요청) + 폰트 체계 교체: 고운바탕 제거, UI Pretendard 단일, 아트 영역만 tvN Enjoystories 셀프호스팅
- 콜라주 개편: '내 배치' 탭 제거 → 3템플릿 통합 드래그 편집(CollageBoard), 문구 스티커 신규(3스타일), 저장 폰/PC 2분할, 배치 1:1 캔버스 export, legacy 마이그레이션
- 온보딩 Act4 간격(flex-1 잉여 공간 구멍 수정)·보드 슬롯 정사각·리뷰 작성하기/수정하기 분기·단계 리네이밍, Playwright 3뷰포트 검증(scripts/verify-v615.mjs)

### 2026-06-11 (v6.14 피드백 5건 반영 + 푸시·프로덕션 배포)
- 팔레트 선명 톤: 뮤트가 칙칙하다는 피드백 → 명도 대신 채도를 올려 밝게, 크림·흰색 4.5:1 유지 (colors.ts만 수정)
- 폰트 불일치 원인 규명: `text-display` 크기 토큰에 `font-display` 서체 클래스 누락 8곳 수정 + 페어링 규칙 명문화 + `check-typography.js` 가드 신설
- 온보딩: Act 4 결구 "그게 비전보드의 힘이야" 명조체 격상, "삶의 방향" 서브텍스트 사용자 카피로 교체(2줄 이내 검증)
- /board 2열×3행 무스크롤(h-dvh, 슬롯 높이 채움), StoryModal 아이콘 트리거 variant
- /collage 템플릿 4종 + 포인터 이벤트 기반 드래그·리사이즈 배치 편집(`components/collage/`, collageLayout 0..1 정규화 스키마)
- 배경화면 PC 타깃(1920×1080) 추가 — wallpaper.ts rect 기반 파라미터화(모바일 출력 픽셀 동일), 파일명 `-pc`
- `verify-v614.js` ALL OK → master 푸시(0d2105c) + `npx vercel --prod` 배포, 프로덕션 4경로 200

### 2026-06-11 (v6.13 디자인 시스템 정립 + 피드백 6건 — 미배포)
- 이름 입력칸 이중선 버그 수정: 전역 `:focus-visible`(unlayered)이 Tailwind v4 `outline-none` 유틸리티를 이기는 캐스케이드 문제 — input/textarea 예외 추가
- 타입 스케일 7토큰(@theme) 정의 후 임의 px·기본 유틸리티 357곳 전면 치환, 도토리 말풍선 Pretendard 통일, `docs/design-system.md` 신설
- Act 4: 리드인 삭제·"원하는 것이 뚜렷해지는 순간" 카피 수정, 비교 이미지 flex 흡수 구조(min-h 4.5rem~max-h 64)로 3뷰포트 무스크롤
- 팔레트 v6.11 뮤트 톤 복귀(4색만 2~9% 최소 다크닝으로 크림 4.5:1 충족), /board 데스크톱 3열 무스크롤
- 배경화면 디자인 2종: wallpaper.ts `style` 파라미터(polaroid/minimal), 미니멀=크림 배경 정렬 그리드+섹션 컬러 도트, WallpaperSheet 스타일 세그먼트
- `scripts/verify-v613.js` ALL OK (커밋 96b5094)

### 2026-06-11 (v6.12 사용자 피드백 6건 반영 — 미배포)
- 온보딩 뷰포트 고정: `h-dvh overflow-hidden`으로 전 Act 페이지 스크롤바 제거, Act 2 채팅 [헤더 고정+내부 스크롤+하단 CTA] 재구조화, `.scroll-soft` 신규
- Act 3 예시 이미지 flex 기반 축소(CTA 상시 가시), Act 4 자동 슬라이드 전용(스와이프·화살표 제거)+검은 박스→Gowun Batang 인용구+압축(812px에 CTA까지 수용)
- 컬러 팔레트 리뉴얼: 선명한 6색 + `lib/colors.ts` 단일 소스(4곳 하드코딩 통합), 틴트 배경 카드→흰 카드+컬러 좌측 보더
- 용어 교체: "장면 그리기"→"미래의 하루 그리기" (라벨성 문구 ~15곳, 자연 문장 속 "장면"은 유지)
- `/collage` 배경화면 저장 신규: `lib/wallpaper.ts`(Canvas 렌더)+`WallpaperSheet`(탭 2모드)+`api/image/proxy`, Web Share/다운로드 폴백
- 검증: `scripts/verify-v612.js` — 3개 뷰포트 × Act 0~5 페이지 스크롤바 없음+CTA 확인, 다운로드 이벤트 확인. flex shrink 클리핑 버그(`flex-shrink-0` 필요) 발견·수정

### 2026-06-11 (v6.11 UI/UX 품질+아이덴티티 개편 — 미배포)
- frontend-design × ui-ux-pro-max 조합 리뷰 → Phase A(품질)/B(아이덴티티) 2커밋(27142fc, 003e843)
- 품질: 대비 미달 `#9CA3AF` 전수 치환, focus-visible+포커스 트랩, 모바일 본문 15px, Act 0 진짜 알파 webm(colorkey 생성), UI 이모지→lucide SVG
- 아이덴티티: Gowun Batang 디스플레이 서체, 정원 톤 섹션 6색(4.5:1 검증), 랜딩 히어로 폴라로이드 보드 재설계, 대시보드 폴라로이드 썸네일
- 배포는 사용자 로컬 확인 후 진행하기로 — dev 서버 확인 상태로 세션 종료

### 2026-06-10 (v6.10 Act 0 영상 교체 + 배포)
- `최종3.mp4` → `public/tori-v3.mp4`로 복사, Act 0 영상 소스 교체
- `mix-blend-mode: multiply` + `contrast(1.15) saturate(1.1)` 필터로 배경 CSS 제거
- `vercel --prod` 프로덕션 배포 (3cfd4ad)

### 2026-06-10 (v6.9 온보딩·대시보드·질문 플로우 7건 + 배포)
- 온보딩: Act 0 영상 흰 배경 제거(`mix-blend-mode: multiply` — fadeIn stacking context 때문에 래퍼 배경색 필수), 도토리 문구 흙→땅 2곳, Act 4 다리 문장 다크 콜아웃 카드 강조(삭제 대신 UI 개선)
- 대시보드: 🐿️ → 토리 프로필 이미지, ProcessGuide(전체 과정 보기) 4→5단계 동기화(스토리 추가)
- 질문 플로우: 질문 단계 답변 버블에 인라인 수정(기존 review 패턴 재사용), ProcessBar `usePathname` 경로 인식(`/scenes`를 `/scene`보다 먼저 매칭, maxStep으로 클릭 범위 유지)
- Playwright 검증 로컬·프로덕션 각 15/15 + 회귀 프로브 4건 통과, `npx vercel --prod` 배포 (cced1ac)

### 2026-06-10 (v6.8 온보딩 마감·보드 단계 유도·한눈에 보기 분리 + 배포)
- 온보딩: Act 0 영상 `최종3.mp4`→`tori-final3.mp4`(720p CRF28, 166KB), Act 3 세로 이미지 48vh 제한, Act 4 카루셀 자동 순환·레이아웃 점프 제거(조건부 힌트가 원인)·텍스트 가독성 강화
- 보드: CTA 문구 재설계(/copywriting — 원하는 모습 답하러 가기·원하는 하루, 스토리로 보기), 사진만 올린 섹션 pill 강조(제안 톤), "6개 완성 시 열림" 가이드 삭제
- 한눈에 보기 `/collage` 별도 페이지 분리(데스크톱·모바일 공통), 보드 하단 활성화 버튼(0장이면 안내 문구), 사진 3장 통일, ProcessBar 5단계(스토리 단계 추가)
- 빌드 통과 후 `npx vercel --prod` 배포(2e3d634), 스모크 체크 5개 경로 200

### 2026-06-10 (v6.7 영상 교체·온보딩 모바일·Act4 스와이프·보드 재설계 + 배포)
- Act 0 영상 `최종2.mp4` → ffmpeg 720p CRF28 압축 172KB (`tori-final2.mp4`), Act 1 모바일 재구성(프로필 헤더+전폭 입력), Act 2/3/5 들여쓰기 통일
- Act 4 스와이프 카드: 막연한 바람(앉은 모습·grayscale) ↔ 생생한 장면(러닝) 2슬라이드, 사진 위 그라데이션 오버레이, 설명 한 줄 압축 — Unsplash 후보 17장 육안 선별
- 보드 재설계: CTA 상시 표시 + `lib/sectionRoute.ts` 공용화(not_started→채팅 라우팅 버그 수정), `StoryModal` 팝업(토글 폐기), 미래하루 스토리 보드 노출/수정, 웹 2-pane(sticky 콜라주)
- 스토리 직접 수정(보드·moment), finish "다시 써줘" 덮어쓰기 확인 프롬프트
- Playwright 28체크 통과 후 `vercel --prod` 배포 (4252f2e)

### 2026-06-10 (v6.6 온보딩·채팅·보드 개선 6건 + 프로덕션 배포)
- Act 0 캐릭터 영상 교체: `최종.mp4`(6.2MB) → ffmpeg 720p CRF28 압축 211KB (`tori-final.mp4`)
- Act 3/4 재구성: Unsplash 목업 제거 → 실제 비전보드 예시 이미지(모바일 세로/웹 가로), Act 4에 러닝 사진 + "비전보드를 하면 좋은 이유" 헤더 + 혜택 카드 3개 이동
- 채팅 아바타 🐿️ 이모지 → `프로필상반신.png`, 입력창 회색 배경 → 흰 배경+테두리
- /board "한눈에 보기" 폴라로이드 콜라주 신규 (`VisionBoardCollage.tsx`) — 다크 배경, 미세 회전, 중앙 연도 탭 수정(`boardYear`)
- 모바일 최적화: section `h-screen`→`h-dvh`, ChatInput·finish safe-area, scenes calc 폭→grid, 말풍선 반응형 폭
- Playwright QA 스크립트(`scripts/verify-v66.mjs`)로 11장 스크린샷 검증 후 master 머지(c38a64f) + `vercel --prod` 배포, 스모크 체크 5개 URL 전부 200

### 2026-06-10 (v6.5 온보딩 개선 7건 — QA 피드백 반영)
- Act 0 캐릭터 영상 교체: 배경제거 처리본이 왼쪽 눈을 흰색으로 망가뜨림 → 검은 배경 원본(`인사_0610-2.mp4`)에 `colorkey=black:0.1:0.1` 적용이 더 깨끗 (434KB, 흰배경 fallback MP4 별도 생성)
- 도토리 스토리: 2.5초 자동 진행 타이머 제거 → 탭 전용 (모바일에서 메시지 우르르 나오던 원인이 타이머였음)
- 상단 단계 표시: 라벨형 ChapterProgress → 라벨 없는 점 5개 (몰입 방해 최소화, 컴포넌트 삭제)
- Act 3: 완성 비전보드 미리보기 목업 추가 (6영역 + Unsplash 사진, 이름 개인화, -1.5° 기울임 카드)
- Act 3 CTA "그게 어떻게 가능한 건데?" → "와, 기대되는데?", Act 4 킥커 "이게 왜 효과 있는지 보여줄게."
- /board 빈 슬롯 탭 → 질문 없이 바로 사진 업로드 (× 삭제 포함), 대시보드 "나의 비전보드 보러가기 →" 상시 노출
- 워딩 통일: scenes·moment·section 페이지 존댓말 14곳 → 토리 반말 (랜딩은 존댓말 유지)
- Playwright 모바일 뷰포트(터치) 검증 18체크 전부 통과
- 프로덕션 배포 완료: https://vision-board-web.vercel.app (런타임 에러 0건, 새 WebM 서빙 확인)

### 2026-06-10 (v6.5 온보딩 비전 카피 개선 + 배포)
- 비전 카드 첫 제목 "뇌가 원하는 삶을 현실로 믿게 해줘" → "원하는 삶을 현실로 믿게 해줘"
- Act 3 CTA "어떻게 만드는 건데?" → "그게 어떻게 가능한 건데? →" (다음 단계 원리 설명과 연결)
- WebM video에 `transform: translateZ(0)` + `backfaceVisibility: hidden` 추가 — 눈 하얘짐 렌더링 플리커 완화
- 프로덕션 배포: https://vision-board-web.vercel.app

### 2026-06-09 (온보딩 영상 투명 WebM 교체)
- 온보딩 Act 0 영상을 `인사- 배경없음.mp4` → ffmpeg colorkey(white, similarity=0.15, blend=0.25) 변환 → `인사-투명.webm`으로 교체
- `<video>` 태그: WebM 우선 + MP4 fallback, 280×280 contain, 불필요한 `videoRef`/`useRef` 제거
- 배포 3회 (MP4 직접 → GIF → 투명WebM → 파라미터 조정 재배포)

### 2026-06-09 (v6.4 온보딩 5-Act 리스트럭처링 + 섹션 채팅 단순화)
- **온보딩 재편:** Acts 0-4 구조 — 토리 소개(신규 카피) → 이름 → 도토리 잠재력 스토리(별도 페이지) → 비전보드 시각 설명(정의박스+효과카드3개+인용구) → 6영역 그리드
- **코드 정리:** 버킷리스트·gardenState 완전 제거 (onboarding, storage, types, dashboard, finish)
- **섹션 채팅 단순화:** 도입 3→2 메시지, cushionText+questionText 하나의 말풍선으로 통합, ChatBubble whitespace-pre-line 추가
- **프로덕션 배포:** https://vision-board-web.vercel.app (commit b4f3302)

### 2026-06-09 (온보딩 리스트럭처링 기획 완료 — 구현 전 wrap)
- **온보딩 page.tsx 분석 완료:** 675줄 6-Act 구조 전면 파악, Act 4 정원 진단 코드 영역(l.440-560) 식별
- **리스트럭처링 플랜:** Acts 6→4(0→1→2→3) 축소 플랜 수립 — Act 4(garden 진단) 제거, 버킷리스트(bucketPhase) 제거, Act 2 신규 비전보드 스토리텔링(7개 말풍선) 추가
- **카피 방향:** Act 0 인사 간소화("다음 →"), Act 2 신규 카피(비전보드 정의→시각화 동기부여→삶의 주도권→구체화 예시), 도토리 이야기 은유 재구성
- **미해결 항목:** Image 2(채팅 양방향 말풍선 단순화), Image 4(어색한 한국어 표현 전면 점검)
- **HANDOFF 생성** — 새 세션에서 `/wrap` 후 `HANDOFF_2026-06-09-온보딩-리스트럭처링.md`로 이어서 구현

### 2026-06-09 (v6.3-story code implemented — deploy)
- **온보딩 page.tsx 전면 구현:** Acts 0-5 구조, Tori 첫인사(정원사), 도토리 이야기 6단 말풍선(2.5cm→60m 2,400배, 2.2초 자동 전환), 버킷리스트 유/무 분기, 정원/화단 은유 일관화
- **프롬프트 수정:** `lib/onboarding-prompt.ts` — 도토리/참나무 직접 언급 금지 제거, Tori가 당당히 도토리 세계관을 말하도록 변경
- **회복:** `git checkout HEAD`로 날아간 Acts 0-5 WIP를 대화 기록에서 복원 후 v6.3-story 카피 적용
- **배포:** Vercel preview 배포 완료 (fc680a0, feat/tori-rebrand)
- **Vercel preview:** https://vision-board-qc1il785y-heleneasytask-7494s-projects.vercel.app

### 2026-06-09 (v6.3-story implement)
- 온보딩 스토리텔링 전면 분석: 도토리→참나무 2,400배 성장 은유를 철학 백본으로 채택
- Act별 스토리보드 설계: 인사(영상) → 첫 만남(이름) → 버킷리스트(유/무 분기) → 감정 연결 → 정원 진단 → 6영역 안내
- 버킷리스트 유/무 분기 처리: 없으면 "괜찮아, 지금부터 나와 함께 찾아가자" 위로 플로우
- AI 모델 추천: Claude 3.5 Sonnet (한국어/캐릭터 일관성/공감 최적)
- **코드 구현:** Act 1~5 카피 전면 개선 — 씨앗/정원 은유 자연스럽게 위빙, 감정 곡선(호기심→기대→설렘→뿌듯함→의지) 반영
- **신규 파일:** `lib/onboarding-prompt.ts` — 온보딩 전용 Tori 시스템 프롬프트 (도토리 은유 백본, 한국어 전용, 카카오톡 톤, Act별 감정 곡선 명시)
- **완료 확인:** LSP diagnostics 0 에러, Production build 성공
- **HANDOFF 업데이트:** 2개 HANDOFF 모두 v6.3 구현 완료 상태로 갱신

### 2026-06-09 (v6.2-v6.3 온보딩 UX 개선 + 랜딩 캐러셀)
- 온보딩 Step 2: select phase 제거 — textarea 입력 후 바로 imagine → feeling → connect 직행 (BucketPhase.SELECT → 제거)
- "버킷리스트 없어요" fallback: 빈 textarea → 토리 위로 메시지 → noBucketPhase → Step 3 이동
- `globals.css`: `@keyframes float` (4s, 8px) + `@keyframes breath` (3s, scale 1.05) 추가
- Step 1 토리 이미지: `animate-float` 클래스 적용 (부드러운 상하 애니메이션)
- 랜딩 페이지: 예시 비전보드 오토 롤링 캐러셀 (Unsplash 3장, 4초 자동 전환, 닷 네비게이션, hover 일시정지)
- 캐러셀 verification.png: Playwright로 실제 렌더링 확인 완료
- 배운 점: `.next` 캐시 purge가 필요한 상황 인지; Start-Process → npm은 `cmd.exe` 경유 필요 (Windows)

### 2026-06-08 (v6.1 온보딩 카피 + 토리 이미지)
- 온보딩 Step 1: 이모지 박스 → 실제 토리 일러스트(`public/tori-gardener.png`) 교체
- Step 1 카피: 도토리→참나무 잠재력 비유 추가 + 다정한 어조 ("안녕? 나는 토리야. … 네가 어떤 참나무로 자라날지 나 정말 궁금해.") — /copywriting + /marketing-psychology 원칙 적용
- Step 2 input/connect: Step 1 비유와 자연스럽게 연결, connect phase "그 장면들이 모이면 네 참나무 모양이 보여" 마무리
- Vercel 프로덕션 배포 완료 (commit 77da487)

### 2026-06-04 (v3.8 이미지 섹션 전면 개선)
- **버그 수정 2개:** AI 이미지 3장 동일 문제 → describe/generate API에 와이드샷/미디엄샷/클로즈업 구도 강제 + 컴퓨터 장면 최대 1개 제한; images 단계 묘사 카드 중복 표시(opacity-60) 완전 제거
- **통합 5슬롯 갤러리:** AI/업로드 구분 없는 3+2 그리드, 어느 슬롯이나 사진 업로드 가능, AI 슬롯 개별 삭제 후 교체 가능, uploadedImages 5개로 확장 + resetAiImages 함수 추가
- **UX 개선:** 묘사 카드에 ✏ 아이콘 + hover/focus border + placeholder 추가; images 단계 스크롤 ~2000px → ~720px (스토리/묘사 블록 숨기고 접기/펼치기 요약 칩으로 대체)

### 2026-06-03 (v3.5 섹션 UX 개선 + 이미지 복구)
- **섹션 레이아웃 전면 개편:** `h-screen` 고정 뷰포트, 채팅 `flex-1 overflow-y-auto` 독립 스크롤, 입력창 sticky 하단 고정, JS `onMouseEnter/Leave`로 스크롤바 hover 토글 (CSS-only 방식은 Chrome에서 불안정)
- **섹션 UX 추가:** 헤더 `←` 뒤로가기 + `✓ 저장됨` 2초 인디케이터, 질문 단계 사용자 버블에 인라인 수정(textarea) 기능
- **이미지 생성 복구:** dall-e-3 → `gpt-image-1` 원복 (1024×1024 URL 방식); Vercel 프로덕션 배포 완료 (commit db541f0)

### 2026-06-03 (v3.4 이미지 저장·인라인 수정·반응형)
- **이미지 저장:** `dall-e-2 + b64_json` 전환 → JPEG 압축(`imageUtils.ts`) → localStorage 영구 저장; board 페이지 레거시 `images` → `generatedImages` 버그 수정; "저장 중..." 로딩 상태 추가
- **인라인 답변 수정:** 섹션 review 화면 개별 답변마다 "수정" 버튼 → textarea 전환 → 저장; 하위 데이터(장면/이미지) 있으면 "다시 만들기" 경고 표시 (marketing psychology: IKEA Effect + Commitment)
- **반응형 레이아웃:** `maximumScale` 제거, 전 페이지 `max-w-md md:max-w-xl`, 대시보드 2열 그리드, 비전보드 2열 섹션 그룹; Vercel 프로덕션 배포 완료

### 2026-06-02 (v3.3 AI 스택 전환 + 스토리 서술체)
- 스토리 API: Groq(llama) → OpenAI gpt-4o-mini, 프롬프트 톤 "반말" → 서술체("나는 ~한다, ~느낀다")
- 이미지 API: Groq 의존성 완전 제거, 프롬프트 변환 gpt-4o-mini, 이미지 생성 dall-e-3 → gpt-image-1
- 이미지 생성 디버깅: 400 "dall-e-3 does not exist" 원인 파악, gpt-image-1 전환 후에도 실패 — 미해결

### 2026-06-02 (v3.2 UX 흐름 개선, 수정 기능, 스토리 품질)
- **Plan 모드:** 6가지 이슈 분석 — marketing-psychology (Peak-End Rule·Hick's Law·Commitment·IKEA Effect) 프레임 적용
- **버그 수정:** 대시보드 status 기반 스마트 라우팅, moment step resume 버그(`situationText`→story step), 이미지 에러 핸들링(missing_key/failed 구분 + "글만 저장" fallback)
- **UX 개선:** Scene 2-버튼 분기 → 단일 CTA, Section review "다른 섹션 먼저 하기" 텍스트 링크 격하, 수정 메뉴 (4단계 cascade: 이미지/스토리/장면/답변)
- **AI 품질:** llama-3.1-8b → llama-3.3-70b-versatile, 사용자 표현 보존 프롬프트(200-300자, 격상 금지), storage.ts reset 함수 4개 추가; Production 배포 완료

### 2026-06-02 (v3.1 스토리 품질·흐름·이미지 강화)
- `api/story/section`: 프롬프트 전면 개선 — "복사 금지" 지침 삭제→확장 허용, 350-450자, 아침→저녁 구조, **볼드** 지침, temperature 0.85
- `moment/[id]`: `renderStory()` 마크다운 볼드 렌더링, `usedAdditional` 추가입력 1회 제한, placeholder 가이드 텍스트 개선
- `scene/[id]`: 버튼 카피 "구체적인 순간 담고 하루 그리기 →" + 서브텍스트 (흐름 명확화); `image/generate`: `Promise.allSettled` 부분 성공, JSON 파싱 fallback
- v2.4c 구식 플랜 파일 삭제, `OPENAI_API_KEY` Vercel 등록 + v3.0 첫 프로덕션 배포

### 2026-06-02 (v3.0 /moment/[id] — 상황 묘사·미니 스토리·DALL-E 이미지)
- `/moment/[id]` 신규 페이지: 3단계(상황 묘사 → Groq 미니스토리 → DALL-E 3 이미지 3장), 재생성·라이트박스 포함
- `/api/story/section`, `/api/image/generate` 신규 API; `openai` npm 패키지 설치
- `lib/types.ts·storage.ts·questions.ts` 확장 (situationText/miniStory/generatedImages 필드, 6섹션 situationChips); scene 라우팅 `/moment/[id]`로 변경

### 2026-06-02 (카피 개선 7개 이슈 + 배포)
- `questions.ts`: introText 6개 맥락 연결, current questionText 3개 어조 개선, example 4개 긍정/혼합 교체, helpQuestion "③" 참조 제거
- `scene/[id]`: cushionText 설명+이유 추가·keyword 반복 제거, 멀티힌트, 3선택지→2선택지+서브텍스트
- v2.9 전체 Vercel 프로덕션 배포 완료 (commit fd35a5c)

### 2026-06-02 (v2.9 UX 전면 개편 — 랜딩·프로세스오버뷰·섹션·장면)
- 랜딩 페이지 구조 재설계: Hero 차별점 강조 + Contrast 섹션(기존 비전보드 vs lumi) 신규 + How it works·CTA 카피 전면 교체
- `/welcome` 신규 페이지: 온보딩 후 4단계 타임라인(발견→장면→이미지→완성) + 6색점 + 기존 사용자 skip (`welcomeSeen` 플래그)
- 섹션 UX: 브릿지 쿠션 메시지, InlineInput example·hint prop, 도움말 패널 연결, 리뷰 라벨+값 행 레이아웃, 대시보드 카드 키워드 서브라인
- 장면 페이지: keyword 기반 동적 질문, 완성 후 3선택지(이미지 지금/다른 영역/나중에 한꺼번에)

### 2026-06-01 (v2.8 섹션 카피 개선 구현 + 배포)
- `types.ts` `shortTitle?` / `whyText` 필드 추가, `questions.ts` 6개 introText "이번엔" 제거 + Section 1 `shortTitle: '나 자신'` + 6개 `whyText` 공감 버블 신규
- `dashboard`: 섹션 카드 제목 shortTitle 우선 표시, `section/[id]`: introText 다음 whyText 버블 노출, `SectionComplete`: "{title} 이야기, 다 썼어." 패턴 통일
- Vercel 프로덕션 배포 완료 (commit 99e3284)

### 2026-06-01 (섹션 카피 개선 — 플랜 완성, 구현 대기)
- /copywriting + /marketing-psychology 스킬 적용 — 대시보드 "나" 단독 표시, 섹션 introText "이번엔" 패턴, SectionComplete 문구 문제 분석
- 최종 플랜 확정: `shortTitle` 필드 추가(Section 1 → "나 자신"), 6개 `introText` 전면 교체(패턴 다양화), 6개 `whyText` 신규(공감 메시지), SectionComplete " 이야기" suffix
- 변경 대상: `lib/types.ts`, `lib/questions.ts`, `app/dashboard/page.tsx`, `app/section/[id]/page.tsx`, `SectionComplete.tsx` — 구현 미진행

### 2026-05-31 (v2.7 섹션 입력 + 장면 페이지 개편)
- v2.7 섹션 입력 UX 전면 개편: AI 채팅 제거 → 고정 질문 4개 + 내러티브 리뷰 방식으로 전환
- 장면 페이지 개편: AI 채팅 제거 → 이전 답변 컨텍스트 활용 + 고정 질문 방식으로 전환

### 2026-05-31 (v2.6 랜딩 페이지 + UX 전환 개선)
- 랜딩 페이지 신규 (`app/page.tsx`): hero("목표가 없어도 괜찮아.") + problem + how it works + what you get, 기존 사용자 자동 대시보드 리다이렉트
- 온보딩 완료(`handleFinish`) → `/dashboard` 대신 `/section/1` 직행, 대시보드 첫 방문 배너 카피 "어디서부터 해도 괜찮아" → "나 섹션부터 시작해봐"
- 장면 완료 버튼 "이미지 찾으러 가기 →" → 완료 상태 기반 동적 텍스트 (마지막 섹션: "비전보드 완성하기 →", 중간: "대시보드로 돌아가기")

### 2026-05-31 (v2.5 섹션 UX 재설계)
- InlineInput 컴포넌트 신규 (`components/InlineInput.tsx`): 하단 고정 입력창 제거 → lumi 질문 아래 인라인 텍스트박스 (슬라이드 애니메이션, step별 placeholder 예시)
- 도움 버튼 재설계: "다른 질문 형태로 볼게" 제거 → InlineInput 하단 "답변 도와줘" + 섹션별 helpQuestions/example 데이터로 패널 내용 교체, 예시 버튼 → 텍스트
- 섹션 완료 전환: "장면 바로 그려가기" / "다른 섹션 질문 시작하기" + 가이드 설명, Scene 완료 후 dashboard 허브 방식
- AI 버그 수정: STEP 0 하드코딩 "건강" 예시 → 실제 섹션명 주입, "안녕하세요 친구" 인사 버그 프롬프트 수정
- Groq 모델 변경: llama-3.3-70b-versatile (100K TPD 소진) → llama-3.1-8b-instant (500K TPD)

### 2026-05-31 (v2.4 채팅 UX + 입력 개선)
- v2.4: 언어 혼용 차단 강화 (CJK 필터 `stripCJK`, 시스템 프롬프트 최상단 언어 규칙), 예시 형식 개선 (단답+문장형 병렬), `lib/helpContent.ts` 신규, "다른 질문 형태로 볼게" 도움 패널 (스텝당 최대 2회, 예시 카드 탭 전송)
- v2.4b: Enter=줄바꿈/"다 썼어 →" 버튼 전송으로 변경, 가이드 텍스트 placeholder 통합, lumi 질문 주어 "나는"→"너는/[이름]이는" 수정 (userName을 API에 주입)
- v2.4c 플랜 완료(미구현): 점수 범위별 반응 지침 / 말풍선당 역할 1개 원칙 / nextSlot contextNote 주입 / temperature 0.5 / "맞아" 버튼 API 없이 직접 완료 처리

### 2026-05-31 (v2.3 섹션 채팅 UX)
- Groq API 키 Vercel 등록 (Production + Development), 프로덕션 배포
- 섹션 채팅 다중 말풍선 구조: `message: string` → `messages: string[]`, 300ms 스태거 딜레이
- lumi 질문 순서 재설계: 지금 → 원해 → 더 들여다보기 → 방향 키워드
- 프롬프트 품질 개선: 한국어 전용, STEP 브릿지 패턴, 슬롯 추출 강제, 미러링 안전 조건 (4개 슬롯 모두 채워야 진입)
- "잘 모르겠어 😅" 도움 버튼 추가 (lumi 마지막 말풍선 아래, helpQuestions 트리거)

### 2026-05-31 (v2.2 완료)
- Anthropic → Gemini 전체 마이그레이션: `@google/generative-ai` 설치, 4개 API 라우트 교체 (`gemini-1.5-flash`)
- v2.2 플랜 Task 4~10 완료: 구 컴포넌트 6개 삭제, ProcessBar 4-STEP, 온보딩 7단계, `/api/chat/scene`, `scene/[id]` 채팅 UI, `/api/story`, `finish` 4페이즈
- 로컬 실행 후 버그 수정: `gemini-2.0-flash` 무료 쿼터 초과 → `gemini-1.5-flash` 전환, 온보딩 step 7 뒤로가기 버튼 + 첫 질문 프리뷰 문구 수정

### 2026-05-31 (v2.2 재설계 시작)
- PRD v2.2 수령 + 구현 플랜 10개 Task 작성 (`docs/superpowers/plans/2026-05-31-v2-chat-redesign.md`)
- Task 1: `lib/types.ts` + `lib/storage.ts` — ChatMessage, ExtractedSlots 타입 + 채팅 저장 함수 5개
- Task 2: `/api/chat/section` — lumi 섹션 채팅 API (슬롯 추출 + JSON 반환)
- Task 3: `ChatBubble.tsx` + `ChatInput.tsx` 공용 컴포넌트 생성
- Task 4 진행 중: `section/[id]/page.tsx` 채팅 UI로 재작성 완료, 기존 컴포넌트 삭제 전 인터럽트

### 2026-05-31
- `Playground/flowchart.html` v0.7 → v1.0 업데이트: 현재 빌드 기준 전체 플로우 반영
- 전체 플로우: deferred phase(DeferredCheck), /scene 허브(순서 자유), /finish 완성 페이지, AI 요약 카드 추가
- 섹션 상세 탭: PHASE 3+4 제거, 최신 질문 텍스트(questions.ts) + 컬러(관계#F59E0B/돈#F97316/공간#06B6D4) 동기화
- 로컬 HTTP 서버(포트 8765) 실행 → 크롬에서 라이브 URL로 오픈

### 2026-05-30 (3차)
- UI/UX Pro Max 스킬 적용: 디자인 시스템 분석 + 접근성 이슈 발견 (`cursor-pointer` 누락, `prefers-reduced-motion` 미지원)
- `globals.css`: `button { cursor: pointer }` 전역 + `prefers-reduced-motion` 추가
- 카피 완료: onboarding Step 5(페인포인트+효과), review 헤더, scene 인트로/이미지 안내 lumi 톤
- 대시보드 상태 명확화: 2단계 도트 + 완성 카드 배경색 + "글 완료/✓ 완성" 라벨
- 온보딩 UI 폴리시: lumi 아이콘 그라디언트+그림자, Step 4 버튼 개선, Step 5 컬러 바
- Vercel 배포 2회 완료

### 2026-05-30 (2차)
- 전체 카피 리뷰 + lumi 톤 최종 정리: 온보딩 Step1 훅 강화, Step4 불필요한 skip 버튼 삭제, Step5 비전보드 효과 담은 문구로 교체
- 섹션 subtitle 3개 교체: 일·성장 → "어떤 일로 배우며 성장하고 싶어?", 돈·생활 → "여유로운 상태에서 어떤 선택을 하고 싶어?", 공간·환경 → 질문형으로
- Review 헤더 + AI 서브텍스트 교체, Scene 마이크로카피 lumi 톤, SlotQuestion 마지막 버튼 교체
- Vercel 배포 완료

### 2026-05-30
- lumi 캐릭터 방향 확정: "빛을 비추는 안내자" (Option B) + 친근한 말투 (Option C) 혼합
- 랜딩 페이지 카피 전체 작성: 헤드라인 3안 → "비전보드, 목표가 없어도 만들 수 있어요" 채택
- lumi 온보딩 스크립트 v2 작성 + 코드 적용 (Step 1~5 전면 개선)
- 전체 마이크로카피 lumi 톤으로 교체: SlotQuestion, PhaseReview, SectionComplete, Dashboard, Board, Finish, questions.ts subtitle 6개
- 건강 섹션 subtitle 정렬 이슈 수정 (짧은 1줄 → 질문형으로 교체)

### 2026-05-29 (2차)
- 실기기 피드백 반영 UX 전면 개선: 섹션 이름 명시, CTA 다양화, Review 1열 레이아웃, Scene 답변 패널 + Tab-to-fill
- Scene done 인터루드: 비전보드 X/18 진행 바 + 다음 섹션 CTA
- Unsplash API 연동: 장면 텍스트 기반 이미지 키워드 검색 (`/api/unsplash`)
- DeferredCheck 렌더 중 setState 버그 수정 (useEffect로 이전)

### 2026-05-29
- Review 페이지 개편: 헤더 카피 변경, 6섹션 2열 그리드, AI 종합 요약 카드 (Claude Haiku `/api/summarize`), 장면 그리기 온보딩 블록
- Scene 페이지 대개편: intro 스텝(맥락 설명 + 이전 답변 요약), 3개 독립 장면 textarea, 이미지-장면 연결 UX
- 대시보드 완료 배지 색상 버그 픽스: 섹션별 색상 → 일관된 녹색
- 섹션 답변 수정 버그 픽스: 완료 후 재진입 시 review 화면으로 시작
- 타입/스토리지 확장: `SectionData.sceneTexts[]`, `saveSectionSceneTexts()`

### 2026-05-28
- Stage 1 Next.js 프로토타입 구현 — 랜딩, 온보딩, 대시보드, 섹션 PHASE 1+2, 비전보드 그리드, FINISH
- lumi 5단계 대화형 온보딩 재설계 (이름 입력 → localStorage 저장, 공감 선택, 초대)
- 섹션 타이틀 PDF 기반 업데이트 (나→감정·성장·정체성, 건강→몸·마음·에너지 등)
- CTA 버튼 로직 개선: 텍스트 입력 시만 활성화 + "잠시 스킵할게요" + 미답변 슬롯 체크 화면
- PHASE 3 분리: 장면 그리기 허브(`/scene`) — 전체 섹션 완료 후 원하는 섹션부터 선택
- Vercel 배포 완료 — https://vision-board-web.vercel.app
- 온보딩 구현 변경사항 문서화 (`온보딩_구현_변경사항_v0.2.md`)

### 2026-05-27 (2차)
- PRD v0.1 작성 — 기능 명세, 데이터 모델, 이미지 옵션 비교, 기획 허점 20개 분석
- PRD v0.2 업데이트 — D4/D6 충돌 해소("나중에 답할게요"), 단계별 빌드 플랜 STAGE 1~5
- question_flow.md 완성 — 6섹션 × 슬롯별 메인 질문 + placeholder + 예시 + 서브질문 전체 확정
- 기술 스택 확정: Next.js + Supabase + Vercel

### 2026-05-27
- 비전보드_플로우_v0.6.md 분석 + Playground/docs/ 저장
- 박지현 PDF 6섹션 이미지 분석 → 섹션 내 올바른 흐름 확정 (①②③⑤→리뷰→④→⑥)
- 인터랙티브 플로우차트 HTML 제작 (전체 플로우차트 탭 + 6섹션 컬러 탭)
- GitHub 레포 생성 + Pages 배포 (https://helenmellybelly.github.io/vision-board/)
